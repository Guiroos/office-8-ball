---
phase: 05-user-profiles-advanced-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/time-period.ts
  - src/lib/time-period.test.ts
  - src/lib/profile-stats.ts
  - src/lib/profile-stats.test.ts
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, RANK-05]
must_haves:
  truths:
    - "Perfil pode ser calculado no servidor sem fetch client para métrica principal."
    - "Partidas jogadas do usuário não são duplicadas quando participa dos dois lados."
    - "Semana e mês usam recortes de tempo consistentes para ranking."
  artifacts:
    - "src/lib/profile-stats.ts exporta agregação de perfil reutilizável."
    - "src/lib/time-period.ts exporta resolução de janela all|month|week."
    - "src/lib/types.ts exporta tipos compartilhados para profile e period."
  key_links:
    - "profile-stats usa TeamRecord/MatchRecord e regras D-05..D-08."
    - "ranking consumirá resolvePeriodWindow() para filtrar partidas por período."
---

<objective>
Criar os contratos e módulos de domínio da Phase 5 para perfil agregado e filtros temporais.

Purpose: estabelecer base testável para PROF-01/02/03 e RANK-05 antes de wiring de rotas/UI.
Output: tipos compartilhados + `profile-stats.ts` + `time-period.ts` com testes unitários.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
@.planning/phases/05-user-profiles-advanced-features/05-RESEARCH.md
@src/lib/types.ts
@src/lib/stats.ts
@src/lib/teams.ts

<interfaces>
From `src/lib/stats.ts`:
```ts
export function computeTeamStats(teamId: string, allMatches: MatchRecord[]): TeamStats;
```

From `src/lib/teams.ts`:
```ts
export async function listUserTeams(userId: string, includeArchived?: boolean): Promise<TeamRecord[]>;
```

From `src/lib/types.ts`:
```ts
export type TeamRecord;
export type MatchRecord;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Definir contratos compartilhados de perfil e período</name>
  <read_first>
    - src/lib/types.ts
    - .planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
    - AGENTS.md
  </read_first>
  <files>src/lib/types.ts</files>
  <action>Adicionar tipos explícitos para Phase 5 em `src/lib/types.ts`: `RankingPeriod = "all" | "month" | "week"`, `ProfileTeamStatsRow`, `ProfileAggregateStats`, e `ProfilePageData`. Manter `ProfileResponse` (API editável) intacto per D-02. Referenciar D-01 e D-04 no bloco de comentário dos novos tipos.</action>
  <acceptance_criteria>
    - `rg -n 'export type RankingPeriod = "all" \\| "month" \\| "week"' src/lib/types.ts`
    - `rg -n 'export type ProfileAggregateStats' src/lib/types.ts`
    - `rg -n 'export type ProfilePageData' src/lib/types.ts`
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Tipos de perfil/período existem e o contrato de `/api/profile` não foi alterado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar helper temporal e agregador de perfil com semântica locked</name>
  <read_first>
    - src/lib/stats.ts
    - src/lib/teams.ts
    - src/lib/types.ts
    - .planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
    - .claude/rules/testing.md
  </read_first>
  <files>src/lib/time-period.ts, src/lib/time-period.test.ts, src/lib/profile-stats.ts, src/lib/profile-stats.test.ts</files>
  <behavior>
    - Teste 1: `resolvePeriodWindow("all")` retorna janela vazia sem corte de data.
    - Teste 2: `resolvePeriodWindow("week")` usa semana ISO (segunda-domingo) em UTC-3 (D-10).
    - Teste 3: `resolvePeriodWindow("month")` usa mês calendário UTC-3 (D-11).
    - Teste 4: agregação de perfil conta partida única quando usuário está em ambos os times (D-08).
    - Teste 5: agregação inclui times arquivados no histórico (D-07) e usa membership atual (D-06).
  </behavior>
  <action>Criar `src/lib/time-period.ts` com `resolvePeriodWindow(period, now)` retornando `{ startUtc, endUtc }` para `week` e `month` usando regra fixa UTC-3 em `America/Sao_Paulo` per D-10/D-11; `all` retorna sem limites. Criar `src/lib/profile-stats.ts` com função exportada que recebe usuário, times e partidas e produz `ProfilePageData` com wins/losses/winRate/totalMatches deduplicado por `match.id` per D-05..D-08. Escrever `src/lib/time-period.test.ts` e `src/lib/profile-stats.test.ts` cobrindo edge cases descritos no bloco behavior.</action>
  <acceptance_criteria>
    - `rg -n 'export function resolvePeriodWindow' src/lib/time-period.ts`
    - `rg -n 'teamIds.has\\(match\\.teamAId\\) \\|\\| teamIds\\.has\\(match\\.teamBId\\)' src/lib/profile-stats.ts`
    - `rg -n 'new Map\\(matches\\.map\\(\\(match\\) => \\[match\\.id, match\\]\\)\\)' src/lib/profile-stats.ts`
    - `rg -n 'D-08|counts unique matches' src/lib/profile-stats.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/time-period.test.ts src/lib/profile-stats.test.ts</automated>
  </verify>
  <done>Domínio de perfil/período implementado com testes verdes para semântica D-05..D-11.</done>
</task>

</tasks>

<verification>
Executar `npm run test -- src/lib/time-period.test.ts src/lib/profile-stats.test.ts` e `npm run typecheck`.
</verification>

<success_criteria>
Os contratos base da Phase 5 estão estáveis e prontos para serem consumidos por ranking, profile page e rota H2H sem lógica duplicada.
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-profiles-advanced-features/05-01-SUMMARY.md`
</output>
