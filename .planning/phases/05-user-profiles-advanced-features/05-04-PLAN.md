---
phase: 05-user-profiles-advanced-features
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - src/components/ranking/type-tabs.tsx
  - src/components/ranking/period-tabs.tsx
  - src/components/ranking/ranking-view.tsx
  - src/components/ranking/ranking-view.test.tsx
autonomous: true
requirements: [RANK-05]
must_haves:
  truths:
    - "Usuário consegue alternar período mantendo o filtro de tipo na URL."
    - "Estado vazio mantém filtros visíveis sem fallback automático."
    - "Links de filtro preservam deep-link compartilhável."
  artifacts:
    - "src/components/ranking/period-tabs.tsx implementa navegação all/month/week."
    - "src/components/ranking/type-tabs.tsx preserva `period` ao trocar tipo."
    - "src/components/ranking/ranking-view.tsx exibe empty state period-aware."
  key_links:
    - "TypeTabs e PeriodTabs manipulam query string com URLSearchParams (D-12)."
    - "RankingView mostra empty state explícito com filtros visíveis (D-13)."
---

<objective>
Completar RANK-05 na UI de ranking com controles de período e preservação de query params.

Purpose: tornar os filtros temporais utilizáveis no frontend sem quebrar a UX existente de ranking.
Output: tabs de período + ajustes de tabs de tipo + testes comportamentais atualizados.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
@src/components/ranking/type-tabs.tsx
@src/components/ranking/ranking-view.tsx
@src/components/ranking/ranking-view.test.tsx
@src/app/(authenticated)/ranking/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar tabs de período e preservação de query params cruzados</name>
  <read_first>
    - src/components/ranking/type-tabs.tsx
    - src/components/ranking/ranking-view.tsx
    - src/app/(authenticated)/ranking/page.tsx
    - .planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
  </read_first>
  <files>src/components/ranking/type-tabs.tsx, src/components/ranking/period-tabs.tsx, src/components/ranking/ranking-view.tsx</files>
  <action>Criar `PeriodTabs` client component com valores fixos `all`, `month`, `week` (D-09). Atualizar `TypeTabs` para preservar `period` atual ao trocar `type` (D-12). Atualizar `RankingView` para renderizar `TypeTabs + PeriodTabs` em modo disponível e empty-state, mantendo filtros visíveis e sem fallback para all-time quando lista estiver vazia (D-13). Usar copy PT-BR coerente no empty state de período.</action>
  <acceptance_criteria>
    - `rg -n 'export function PeriodTabs' src/components/ranking/period-tabs.tsx`
    - `rg -n 'params\\.set\\(\"period\"' src/components/ranking/type-tabs.tsx`
    - `rg -n 'PeriodTabs' src/components/ranking/ranking-view.tsx`
    - `rg -n 'Nenhum time encontrado' src/components/ranking/ranking-view.tsx`
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>UI oferece filtros de período integrados e mantém visibilidade de filtros em estado vazio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atualizar testes comportamentais do ranking para period + type</name>
  <read_first>
    - src/components/ranking/ranking-view.test.tsx
    - src/components/ranking/ranking-view.tsx
    - src/components/ranking/type-tabs.tsx
    - src/components/ranking/period-tabs.tsx
    - .claude/rules/testing.md
  </read_first>
  <files>src/components/ranking/ranking-view.test.tsx</files>
  <behavior>
    - Teste 1: empty state em `mode=available` mantém tabs de tipo e período visíveis.
    - Teste 2: links de tabs preservam combinação `type+period`.
    - Teste 3: página continua enviando `period` validado para domínio de ranking.
  </behavior>
  <action>Expandir `ranking-view.test.tsx` para validar comportamento de query-link combinada (`/ranking?type=solo&period=month`), presença dos dois grupos de tabs no empty state e wiring de `RankingPage` com `period`. Preservar testes existentes de podium e links de time.</action>
  <acceptance_criteria>
    - `rg -n 'period' src/components/ranking/ranking-view.test.tsx`
    - `rg -n 'type=solo&period=month|period=week' src/components/ranking/ranking-view.test.tsx`
    - `rg -n 'empty-state|Nenhum time encontrado' src/components/ranking/ranking-view.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/components/ranking/ranking-view.test.tsx</automated>
  </verify>
  <done>Testes do ranking cobrem filtro temporal e preservação de URL junto ao filtro de tipo.</done>
</task>

</tasks>

<verification>
Executar `npm run test -- src/components/ranking/ranking-view.test.tsx` e `npm run typecheck`.
</verification>

<success_criteria>
RANK-05 está completo na UI: usuário filtra por período com URL estável e estados vazios claros.
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-profiles-advanced-features/05-04-SUMMARY.md`
</output>
