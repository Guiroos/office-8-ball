---
phase: 05-user-profiles-advanced-features
plan: 05
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/lib/head-to-head.ts
  - src/lib/head-to-head.test.ts
  - src/app/(authenticated)/head-to-head/page.tsx
  - src/components/head-to-head/head-to-head-view.tsx
  - src/app/(authenticated)/head-to-head/page.test.tsx
  - middleware.ts
autonomous: true
requirements: [PROF-01]
must_haves:
  truths:
    - "Página `/head-to-head` funciona com deep-link `teamA/teamB`."
    - "Params inválidos não quebram rota; página exibe validação e fallback."
    - "Seleções Team A/Team B sincronizam URL sem reload completo."
  artifacts:
    - "src/lib/head-to-head.ts resolve par válido + warning + métricas."
    - "src/app/(authenticated)/head-to-head/page.tsx adiciona rota autenticada."
    - "src/components/head-to-head/head-to-head-view.tsx controla seletores sincronizados com URL."
    - "middleware.ts protege `/head-to-head`."
  key_links:
    - "/head-to-head searchParams -> resolveValidPair -> render warning/fallback (D-15/D-16)."
    - "seletores Team A/Team B -> query params `teamA`/`teamB` (D-17/D-18)."
---

<objective>
Implementar rota dedicada de head-to-head com fallback robusto e URL compartilhável.

Purpose: cumprir decisões locked D-14..D-18 sem depender apenas da seção H2H em `/times/[id]`.
Output: módulo de domínio H2H dedicado + rota autenticada + UI com sincronização de query params.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
@src/lib/team-details.ts
@src/lib/stats.ts
@src/app/(authenticated)/times/[id]/page.tsx
@src/components/teams/h2h-section.tsx
@middleware.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar assembler de head-to-head com validação e fallback</name>
  <read_first>
    - src/lib/team-details.ts
    - src/lib/stats.ts
    - src/lib/teams.ts
    - .planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
    - .claude/rules/testing.md
  </read_first>
  <files>src/lib/head-to-head.ts, src/lib/head-to-head.test.ts</files>
  <behavior>
    - Teste 1: sem params, retorna primeiro par válido acessível (D-15).
    - Teste 2: params inválidos ou não autorizados retornam warning + fallback válido (D-16).
    - Teste 3: par igual (`teamA===teamB`) é rejeitado com mensagem e fallback (D-17).
  </behavior>
  <action>Criar `src/lib/head-to-head.ts` com função exportada para resolver pares válidos a partir de `teamA/teamB` + lista de times do usuário, calcular métricas via `computeHeadToHead`, e retornar `{ pair, warning, options, summary }`. Garantir fallback determinístico para primeiro par válido quando necessário (D-15/D-16) e proibir par idêntico (D-17). Criar testes unitários em `src/lib/head-to-head.test.ts` cobrindo os 3 comportamentos.</action>
  <acceptance_criteria>
    - `rg -n 'export function resolveHeadToHeadData|export async function getHeadToHeadPageData' src/lib/head-to-head.ts`
    - `rg -n 'teamA === teamB|Seleção inválida|fallback' src/lib/head-to-head.ts`
    - `rg -n 'D-15|D-16|D-17|fallback' src/lib/head-to-head.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/head-to-head.test.ts</automated>
  </verify>
  <done>Assembler H2H dedicado suporta fallback robusto e está coberto por testes unitários.</done>
</task>

<task type="auto">
  <name>Task 2: Publicar rota /head-to-head protegida e UI com query sync</name>
  <read_first>
    - src/app/(authenticated)/times/[id]/page.tsx
    - src/components/teams/h2h-section.tsx
    - middleware.ts
    - .claude/rules/auth.md
    - .planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
  </read_first>
  <files>src/app/(authenticated)/head-to-head/page.tsx, src/components/head-to-head/head-to-head-view.tsx, src/app/(authenticated)/head-to-head/page.test.tsx, middleware.ts</files>
  <action>Adicionar rota autenticada `/head-to-head` (D-14) com page server-side que resolve usuário autenticado e consome `head-to-head.ts`. Implementar guard explícito de modo duplo na page (`hasDatabaseUrl`): com `DATABASE_URL`, usar fluxo DB-backed autenticado; sem `DATABASE_URL`, usar fluxo in-memory equivalente sem quebrar render nem deep-link (constraint AGENTS). Criar `head-to-head-view.tsx` com dois seletores explícitos (`Team A` e `Team B`) que impedem seleção do mesmo time e atualizam URL (`teamA`, `teamB`) imediatamente sem full reload via navegação client (D-17/D-18). Tratar warning de seleção inválida com copy PT-BR e manter render estável (D-16). Atualizar `middleware.ts` matcher para incluir `/head-to-head/:path*`. Criar `page.test.tsx` cobrindo fallback sem params, warning em params inválidos e cenário sem `DATABASE_URL` com `delete process.env.DATABASE_URL`, `vi.resetModules()` e import dinâmico da rota.</action>
  <acceptance_criteria>
    - `rg -n 'head-to-head' middleware.ts`
    - `rg -n 'Team A|Team B' src/components/head-to-head/head-to-head-view.tsx`
    - `rg -n 'teamA|teamB' src/components/head-to-head/head-to-head-view.tsx`
    - `rg -n 'hasDatabaseUrl|DATABASE_URL' 'src/app/(authenticated)/head-to-head/page.tsx'`
    - `rg -n 'delete process\\.env\\.DATABASE_URL|vi\\.resetModules|await import' 'src/app/(authenticated)/head-to-head/page.test.tsx'`
    - `rg -n 'describe\\(\"HeadToHeadPage\"' 'src/app/(authenticated)/head-to-head/page.test.tsx'`
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/app/(authenticated)/head-to-head/page.test.tsx && DATABASE_URL= npm run test -- src/app/(authenticated)/head-to-head/page.test.tsx</automated>
  </verify>
  <done>Rota dedicada H2H está protegida, deep-linkável e resiliente a query inválida.</done>
</task>

</tasks>

<verification>
Executar `npm run test -- src/lib/head-to-head.test.ts src/app/(authenticated)/head-to-head/page.test.tsx` e `npm run typecheck`.
</verification>

<success_criteria>
Decisões D-14..D-18 estão implementadas por rota dedicada com URL sincronizada e fallback seguro.
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-profiles-advanced-features/05-05-SUMMARY.md`
</output>
