---
phase: 05-user-profiles-advanced-features
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/lib/ranking.ts
  - src/lib/ranking.test.ts
  - src/app/(authenticated)/ranking/page.tsx
autonomous: true
requirements: [RANK-05]
must_haves:
  truths:
    - "Ranking aceita `period=all|month|week` sem quebrar filtro de tipo."
    - "Filtro por período realmente altera query de partidas por data."
    - "Valores inválidos de query são normalizados para modo seguro."
  artifacts:
    - "src/lib/ranking.ts aceita period e aplica janela temporal."
    - "src/app/(authenticated)/ranking/page.tsx parseia `type` e `period` juntos."
    - "src/lib/ranking.test.ts cobre recorte temporal."
  key_links:
    - "/ranking searchParams -> listAllTeamsWithStats(type, period)."
    - "listAllTeamsWithStats -> resolvePeriodWindow(period) -> prisma.match.findMany(where.playedAt)."
---

<objective>
Estender o pipeline de ranking para suportar período (`all|month|week`) no backend e no parsing da página.

Purpose: implementar base de RANK-05 sem regressão de filtros existentes por tipo.
Output: domínio de ranking period-aware + rota `/ranking` com query parsing combinado.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
@src/lib/ranking.ts
@src/lib/ranking.test.ts
@src/app/(authenticated)/ranking/page.tsx
@src/lib/time-period.ts
@src/lib/stats.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar suporte a period no domínio de ranking</name>
  <read_first>
    - src/lib/ranking.ts
    - src/lib/ranking.test.ts
    - src/lib/time-period.ts
    - .planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
    - .claude/rules/testing.md
  </read_first>
  <files>src/lib/ranking.ts, src/lib/ranking.test.ts</files>
  <behavior>
    - Teste 1: `period=all` mantém comportamento atual (sem `playedAt` no where).
    - Teste 2: `period=week` aplica `gte`/`lt` com janela da semana ISO UTC-3 (D-10).
    - Teste 3: `period=month` aplica janela de mês calendário UTC-3 (D-11).
  </behavior>
  <action>Atualizar assinatura para `listAllTeamsWithStats(type?: "solo" | "duo", period?: RankingPeriod)` e usar `resolvePeriodWindow` de `src/lib/time-period.ts` para montar filtro `playedAt` no `prisma.match.findMany` quando period for `week` ou `month` (D-09..D-11). Preservar ordenação estável já existente (wins/winRate/name). Expandir `src/lib/ranking.test.ts` para validar shape do where por período e manter casos antigos verdes.</action>
  <acceptance_criteria>
    - `rg -n 'listAllTeamsWithStats\\(\\s*type\\?: \"solo\" \\| \"duo\",\\s*period\\?: RankingPeriod' src/lib/ranking.ts`
    - `rg -n 'resolvePeriodWindow\\(period' src/lib/ranking.ts`
    - `rg -n 'playedAt: \\{ gte: .* lt: .* \\}' src/lib/ranking.ts`
    - `rg -n 'period=week|period=month|week|month' src/lib/ranking.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/ranking.test.ts</automated>
  </verify>
  <done>Ranking domain aceita período e aplica recorte temporal com testes cobrindo as janelas.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar /ranking para parsear type + period de forma coexistente</name>
  <read_first>
    - src/app/(authenticated)/ranking/page.tsx
    - src/lib/types.ts
    - src/lib/ranking.ts
    - .planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
  </read_first>
  <files>src/app/(authenticated)/ranking/page.tsx</files>
  <action>Alterar parsing de `searchParams` para aceitar `type` (`solo|duo`) e `period` (`all|month|week`) com fallback seguro para `all`/undefined (D-09, D-12). Chamar `listAllTeamsWithStats(parsedType, parsedPeriod)` e enviar `activePeriod` para a camada de UI sem remover `mode` existente.</action>
  <acceptance_criteria>
    - `rg -n 'searchParams\\?: Promise<\\{ type\\?: string; period\\?: string \\}>' 'src/app/(authenticated)/ranking/page.tsx'`
    - `rg -n 'const parsedPeriod = .*\"all\" \\| \"month\" \\| \"week\"' 'src/app/(authenticated)/ranking/page.tsx'`
    - `rg -n 'listAllTeamsWithStats\\(parsedType, parsedPeriod\\)' 'src/app/(authenticated)/ranking/page.tsx'`
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Rota de ranking propaga filtros de tipo e período para o domínio sem regressão.</done>
</task>

</tasks>

<verification>
Executar `npm run test -- src/lib/ranking.test.ts` e `npm run typecheck`.
</verification>

<success_criteria>
RANK-05 passa a existir no backend/route: query params de período alteram o recorte de partidas de forma determinística.
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-profiles-advanced-features/05-03-SUMMARY.md`
</output>
