---
phase: 05-user-profiles-advanced-features
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/app/(authenticated)/profile/page.tsx
  - src/components/profile/profile-page.tsx
  - src/app/(authenticated)/profile/page.test.tsx
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03]
must_haves:
  truths:
    - "Usuário vê no /profile métricas agregadas reais (wins, losses, win rate, partidas)."
    - "Perfil lista os times do usuário com stats por time."
    - "Render principal do perfil é server-first sem fetch client para métricas."
  artifacts:
    - "src/app/(authenticated)/profile/page.tsx monta dados server-side."
    - "src/components/profile/profile-page.tsx renderiza payload agregado."
    - "src/app/(authenticated)/profile/page.test.tsx valida wiring do RSC."
  key_links:
    - "/profile -> módulo de agregação em src/lib/profile-stats.ts."
    - "ProfileEditDialog continua usando PUT /api/profile sem mistura de métricas."
---

<objective>
Migrar `/profile` para fluxo server-first consumindo agregação de domínio e removendo placeholders hardcoded.

Purpose: cumprir PROF-01/02/03 com rendering consistente e sem fetch client principal.
Output: rota `/profile` server-side + UI de perfil consumindo `ProfilePageData`.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
@.planning/phases/05-user-profiles-advanced-features/05-RESEARCH.md
@src/app/(authenticated)/profile/page.tsx
@src/components/profile/profile-page.tsx
@src/components/profile/profile-edit-dialog.tsx
@src/app/api/profile/route.ts
@src/lib/profile-stats.ts
@src/lib/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wiring server-side da rota /profile com payload agregado</name>
  <read_first>
    - src/app/(authenticated)/profile/page.tsx
    - src/lib/profile-stats.ts
    - src/lib/auth.ts
    - src/app/api/profile/route.ts
    - .planning/phases/05-user-profiles-advanced-features/05-CONTEXT.md
  </read_first>
  <files>src/app/(authenticated)/profile/page.tsx, src/app/(authenticated)/profile/page.test.tsx</files>
  <behavior>
    - Teste 1: com `DATABASE_URL` ausente, página mostra estado indisponível sem crash.
    - Teste 2: usuário autenticado recebe payload agregado e renderiza `<ProfilePage />`.
    - Teste 3: usuário não autenticado não acessa conteúdo de perfil.
  </behavior>
  <action>Transformar `src/app/(authenticated)/profile/page.tsx` em async RSC que: valida `hasDatabaseUrl`, autentica com `getAuthenticatedUser`, busca payload via `profile-stats.ts` (D-01/D-03/D-04) e passa props para componente de apresentação. Criar `src/app/(authenticated)/profile/page.test.tsx` com mocks de auth e profile-stats cobrindo os 3 comportamentos listados.</action>
  <acceptance_criteria>
    - `rg -n 'export default async function ProfileRoute' 'src/app/(authenticated)/profile/page.tsx'`
    - `rg -n 'hasDatabaseUrl|getAuthenticatedUser' 'src/app/(authenticated)/profile/page.tsx'`
    - `rg -n 'profile-stats|ProfilePageData' 'src/app/(authenticated)/profile/page.tsx'`
    - `rg -n 'describe\\(\"ProfileRoute\"' 'src/app/(authenticated)/profile/page.test.tsx'`
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/app/(authenticated)/profile/page.test.tsx</automated>
  </verify>
  <done>/profile obtém dados no servidor e possui cobertura de rota para os estados críticos.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever ProfilePage para consumir props agregadas e remover hardcode legado</name>
  <read_first>
    - src/components/profile/profile-page.tsx
    - src/components/profile/profile-edit-dialog.tsx
    - src/components/primitives/stat-tile.tsx
    - .claude/rules/ui-components.md
  </read_first>
  <files>src/components/profile/profile-page.tsx</files>
  <action>Remover fetches client para `/api/profile` e `/api/matches` do fluxo principal; converter `ProfilePage` para receber `ProfilePageData` por props do RSC (D-01, D-03). Exibir valores concretos para `Vitórias`, `Derrotas`, `Win Rate`, `Partidas` e listar times do usuário com stats por time (PROF-01/02/03). Eliminar constante `TEAMS` hardcoded. Manter `ProfileEditDialog` funcional para edição de campos de conta via `PUT /api/profile` (D-02).</action>
  <acceptance_criteria>
    - `rg -n 'const TEAMS = \\[' src/components/profile/profile-page.tsx` retorna vazio
    - `rg -n 'fetch\\(\"/api/profile\"\\)|fetch\\(\"/api/matches\"\\)' src/components/profile/profile-page.tsx` retorna vazio
    - `rg -n 'stats\\.wins|stats\\.losses|stats\\.winRate|stats\\.totalMatches' src/components/profile/profile-page.tsx`
    - `rg -n 'ProfileEditDialog' src/components/profile/profile-page.tsx`
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Perfil renderiza métricas agregadas e times reais sem hardcodes, preservando edição de perfil.</done>
</task>

</tasks>

<verification>
Executar `npm run test -- src/app/(authenticated)/profile/page.test.tsx` e `npm run typecheck`.
</verification>

<success_criteria>
`/profile` cumpre PROF-01/02/03 com renderização server-first e sem dependência de placeholders legados.
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-profiles-advanced-features/05-02-SUMMARY.md`
</output>
