---
phase: 07-team-details-access-member-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/team-details.ts
  - src/lib/team-details.test.ts
  - src/components/teams/team-detail-access-denied.tsx
  - src/components/teams/team-detail-access-denied.test.tsx
  - src/app/(authenticated)/times/[id]/page.tsx
autonomous: true
requirements: [TEAM-02]
must_haves:
  truths:
    - "Apenas membros atuais do time recebem o payload completo de `/times/[id]`."
    - "Usuário autenticado que não pertence ao time vê um estado explícito de acesso negado, e não um 404 silencioso."
    - "Times ausentes ou arquivados continuam resultando em `notFound()`."
    - "Sem `DATABASE_URL`, a rota continua exibindo o estado indisponível já existente."
  artifacts:
    - "src/lib/team-details.ts expõe um contrato explícito para `detail`, `forbidden` e `not-found`."
    - "src/lib/team-details.test.ts cobre membership gate e garante que queries pesadas não rodam para não membros."
    - "src/components/teams/team-detail-access-denied.tsx renderiza a UX explícita de acesso negado com CTA para `/times`."
    - "src/app/(authenticated)/times/[id]/page.tsx diferencia `forbidden` de `not-found` antes de renderizar a view."
  key_links:
    - "page `/times/[id]` -> `getTeamDetailData(teamId, user.id)` -> branch `not-found | forbidden | detail`."
    - "`isTeamMember(teamId, viewerId)` roda antes de ranking/matches/users em `src/lib/team-details.ts`."
    - "`forbidden` -> `TeamDetailAccessDenied`; `detail` -> `TeamDetailView`; `not-found` -> `notFound()`."
---

<objective>
Trocar o contrato do detalhe de time de “qualquer autenticado ativo” para “membro atual somente”, com UX explícita para acesso negado.

Purpose: fechar o gap principal da Phase 7 sem tocar nas APIs já existentes de membros.
Output: loader server-side com gate de membership + tela de acesso negado + testes focados.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-team-details-access-member-actions/07-CONTEXT.md
@.planning/phases/07-team-details-access-member-actions/07-RESEARCH.md
@.planning/phases/07-team-details-access-member-actions/07-VALIDATION.md
@src/app/(authenticated)/times/[id]/page.tsx
@src/lib/team-details.ts
@src/lib/team-details.test.ts
@src/app/api/teams/[id]/route.ts
@src/components/route-state-screen.tsx
@src/components/route-state-screen.test.tsx

<interfaces>
Current page entry:
```ts
export default async function TeamDetailPage({
  params,
}: {
  params: Promise<{ id: string }>;
})
```

Current loader:
```ts
export async function getTeamDetailData(
  teamId: string,
  viewerId: string,
): Promise<TeamDetailData | null>
```

Membership guard reference:
```ts
const member = await isTeamMember(id, user.id);
if (!member) {
  return NextResponse.json<ApiErrorResponse>(
    { error: "Acesso negado." },
    { status: 403 },
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redefinir o contrato server-side de `getTeamDetailData` para `detail | forbidden | not-found`</name>
  <read_first>
    - src/lib/team-details.ts
    - src/lib/team-details.test.ts
    - src/lib/teams.ts
    - src/app/api/teams/[id]/route.ts
    - .planning/phases/07-team-details-access-member-actions/07-CONTEXT.md
    - .planning/phases/07-team-details-access-member-actions/07-RESEARCH.md
  </read_first>
  <files>src/lib/team-details.ts, src/lib/team-details.test.ts</files>
  <behavior>
    - `team` inexistente ou `archived` retorna `kind: "not-found"`.
    - `team` ativo, mas viewer não membro, retorna `kind: "forbidden"` e não executa queries de ranking, users ou matches.
    - viewer membro recebe `kind: "detail"` com o payload existente de stats, roster, recent matches e H2H.
  </behavior>
  <action>Editar `src/lib/team-details.ts` para exportar `type TeamDetailResult = { kind: "not-found" } | { kind: "forbidden"; teamId: string } | { kind: "detail"; data: TeamDetailData }`. Manter `TeamDetailData` e o assembler atual, mas inserir `isTeamMember(teamId, viewerId)` logo após verificar que o time existe e está `active`. Se `isTeamMember` retornar `false`, devolver `{ kind: "forbidden", teamId }` antes de chamar `listAllTeamsWithStats`, `listUserTeams`, `prisma.match.findMany` ou `prisma.user.findMany`. Se o viewer for membro, devolver `{ kind: "detail", data: { ...payloadAtual } }` sem mudar os campos funcionais do detalhe. Atualizar `src/lib/team-details.test.ts` para substituir asserções antigas de acesso irrestrito por casos explícitos para `not-found`, `forbidden` e `detail`, incluindo um teste que verifica `mockListAllTeamsWithStats`, `mockListUserTeams`, `mockMatchFindMany` e `mockUserFindMany` não chamados quando o resultado for `forbidden`.</action>
  <acceptance_criteria>
    - `rg -n 'type TeamDetailResult|kind: \"forbidden\"|kind: \"detail\"|kind: \"not-found\"' src/lib/team-details.ts`
    - `rg -n 'isTeamMember\\(' src/lib/team-details.ts`
    - `rg -n 'forbidden|not-found|does not call|not.toHaveBeenCalled' src/lib/team-details.test.ts`
    - `npm run test -- src/lib/team-details.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/team-details.test.ts</automated>
  </verify>
  <done>O loader do detalhe diferencia acesso negado de time ausente e só monta dados completos para membros atuais.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Renderizar uma tela dedicada de acesso negado em `/times/[id]`</name>
  <read_first>
    - src/app/(authenticated)/times/[id]/page.tsx
    - src/components/route-state-screen.tsx
    - src/components/route-state-screen.test.tsx
    - src/lib/team-details.ts
    - .planning/phases/07-team-details-access-member-actions/07-CONTEXT.md
  </read_first>
  <files>src/components/teams/team-detail-access-denied.tsx, src/components/teams/team-detail-access-denied.test.tsx, src/app/(authenticated)/times/[id]/page.tsx</files>
  <behavior>
    - Non-member autenticado vê uma tela explícita de acesso negado com retorno para `/times`.
    - `not-found` continua usando `notFound()`.
    - Branch sem banco configurado permanece igual ao comportamento atual.
  </behavior>
  <action>Criar `src/components/teams/team-detail-access-denied.tsx` como wrapper server-safe sobre `RouteStateScreen` com estes props exatos: `code=\"403\"`, `eyebrow=\"Acesso ao time\"`, `title=\"Voce nao faz parte deste time.\"`, `description=\"Somente membros atuais podem abrir os detalhes deste time.\"`, `detail=\"Peca um convite a um membro atual ou volte para a lista dos seus times.\"`, `nextStep=\"Abra /times e escolha um time do qual voce ja participa.\"`, `primaryAction={{ label: \"Voltar para meus times\", href: \"/times\", icon: \"back\" }}`, `secondaryAction={{ label: \"Ir para dashboard\", href: \"/dashboard\", icon: \"home\", tone: \"secondary\" }}`. Criar `src/components/teams/team-detail-access-denied.test.tsx` validando os textos principais e os links `/times` e `/dashboard`. Editar `src/app/(authenticated)/times/[id]/page.tsx` para consumir `TeamDetailResult`: `kind === \"not-found\"` chama `notFound()`, `kind === \"forbidden\"` retorna `<TeamDetailAccessDenied />`, `kind === \"detail\"` retorna `<TeamDetailView {...detail.data} />`. Não alterar o branch atual de `!hasDatabaseUrl()`.</action>
  <acceptance_criteria>
    - `rg -n 'TeamDetailAccessDenied|Voce nao faz parte deste time|Voltar para meus times|/dashboard' src/components/teams/team-detail-access-denied.tsx src/components/teams/team-detail-access-denied.test.tsx`
    - `rg -n 'kind === \"forbidden\"|kind === \"not-found\"|detail\\.data' 'src/app/(authenticated)/times/[id]/page.tsx'`
    - `npm run test -- src/components/teams/team-detail-access-denied.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/components/teams/team-detail-access-denied.test.tsx</automated>
  </verify>
  <done>O page `/times/[id]` mostra acesso negado explícito para não membros e preserva os outros branches esperados.</done>
</task>

</tasks>

<verification>
Executar `npm run test -- src/lib/team-details.test.ts src/components/teams/team-detail-access-denied.test.tsx` e `npm run typecheck`.
</verification>

<success_criteria>
O detalhe de time deixa de vazar para não membros e passa a exibir uma UX de acesso negado clara e previsível.
</success_criteria>

<output>
After completion, create `.planning/phases/07-team-details-access-member-actions/07-01-SUMMARY.md`
</output>
