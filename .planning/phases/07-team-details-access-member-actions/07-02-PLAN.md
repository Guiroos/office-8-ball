---
phase: 07-team-details-access-member-actions
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/app/(authenticated)/times/[id]/page.tsx
  - src/components/teams/team-detail-view.tsx
  - src/components/teams/invite-member-dialog.tsx
  - src/components/teams/invite-member-dialog.test.tsx
  - src/components/teams/member-list.tsx
  - src/components/teams/member-list.test.tsx
  - src/app/api/teams/[id]/members/route.test.ts
  - src/app/api/teams/[id]/members/[userId]/route.test.ts
  - src/app/api/users/route.test.ts
  - e2e/team-member-actions.spec.ts
autonomous: true
requirements: [TEAM-02]
must_haves:
  truths:
    - "Membro atual do time consegue convidar outro usuário por username sem sair de `/times/[id]`."
    - "Lista de membros expõe `Remover` com confirmação somente para membros removíveis."
    - "Sucesso e erro de add/remove usam toast e `router.refresh()` no próprio detalhe."
    - "Fluxo autenticado de gerenciamento de membros e fluxo de acesso negado por URL direta têm cobertura automatizada."
  artifacts:
    - "src/components/teams/invite-member-dialog.tsx implementa lookup por username + POST /api/teams/:id/members."
    - "src/components/teams/member-list.tsx evolui para lista com confirmação inline e DELETE /api/teams/:id/members/:userId."
    - "src/app/api/teams/[id]/members/route.test.ts e src/app/api/teams/[id]/members/[userId]/route.test.ts formalizam os contratos das rotas já existentes."
    - "e2e/team-member-actions.spec.ts prova runtime para manage-members e denied direct access."
  key_links:
    - "`InviteMemberDialog` -> `GET /api/users?username=` -> `POST /api/teams/${teamId}/members` -> `router.refresh()`."
    - "`MemberList` -> confirmação inline -> `DELETE /api/teams/${teamId}/members/${userId}` -> `router.refresh()`."
    - "`page.tsx` passa `viewerId={user.id}` para `TeamDetailView`, que repassa `teamId`, `viewerId`, `team.type`, `team.createdBy` e `members` para os componentes client."
---

<objective>
Conectar as ações reais de convite e remoção de membros ao detalhe do time e provar os fluxos com testes de componente, rota e E2E.

Purpose: fechar a parte interativa da Phase 7 em cima do contrato autorizado definido no plano 07-01.
Output: dialog de convite, remoção inline com confirmação, contratos de rota testados e spec Playwright dedicado.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-team-details-access-member-actions/07-CONTEXT.md
@.planning/phases/07-team-details-access-member-actions/07-RESEARCH.md
@.planning/phases/07-team-details-access-member-actions/07-VALIDATION.md
@.planning/phases/07-team-details-access-member-actions/07-01-PLAN.md
@src/app/(authenticated)/times/[id]/page.tsx
@src/components/teams/team-detail-view.tsx
@src/components/teams/member-list.tsx
@src/components/profile/profile-edit-dialog.tsx
@src/components/profile/profile-edit-dialog.test.tsx
@src/components/teams/team-create-form.tsx
@src/app/api/teams/[id]/members/route.ts
@src/app/api/teams/[id]/members/[userId]/route.ts
@src/app/api/users/route.ts
@src/app/api/users/route.test.ts
@e2e/auth-and-scoreboard.spec.ts
@e2e/helpers/auth.ts

<interfaces>
Member add route:
```ts
POST /api/teams/:id/members
body: { userId: string }
```

Member remove route:
```ts
DELETE /api/teams/:id/members/:userId
```

Username lookup route:
```ts
GET /api/users?username=...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Formalizar contratos das rotas de membros com testes dedicados</name>
  <read_first>
    - .claude/rules/testing.md
    - .claude/rules/api-patterns.md
    - src/app/api/teams/[id]/route.test.ts
    - src/app/api/teams/[id]/members/route.ts
    - src/app/api/teams/[id]/members/[userId]/route.ts
    - src/app/api/users/route.test.ts
  </read_first>
  <files>src/app/api/teams/[id]/members/route.test.ts, src/app/api/teams/[id]/members/[userId]/route.test.ts, src/app/api/users/route.test.ts</files>
  <behavior>
    - POST members cobre 200, 400 payload inválido, 403 não membro, 404 usuário alvo inexistente, 401 sem auth e 503 sem banco.
    - DELETE members cobre 200, 400 erro de domínio propagado, 403 não membro, 401 sem auth e 503 sem banco.
    - `GET /api/users` mantém lookup autenticado; se necessário, expandir apenas com asserts de shape usados pelo dialog.
  </behavior>
  <action>Criar `src/app/api/teams/[id]/members/route.test.ts` e `src/app/api/teams/[id]/members/[userId]/route.test.ts` seguindo exatamente o padrão de mocks de auth dos testes existentes em `src/app/api/teams/[id]/route.test.ts`: mockar `@/lib/auth` com `vi.importActual`, controlar `currentUser`, mockar `@/lib/teams`, usar `process.env.DATABASE_URL = \"postgresql://test\"` no `beforeEach`, apagar no `afterEach`, e importar a rota dinamicamente dentro de cada teste. Para POST, mockar `isTeamMember`, `findUserById` e `addTeamMember`. Para DELETE, mockar `isTeamMember` e `removeTeamMember`. Cobrir os branches 200/400/401/403/404/503 com asserts de status exato e mensagem `{ error }` quando aplicável. Em `src/app/api/users/route.test.ts`, manter os testes atuais e adicionar, se necessário, um assert explícito de shape `user.id`, `user.username`, `user.displayName`, `user.avatarUrl` para refletir o contrato consumido pelo dialog.</action>
  <acceptance_criteria>
    - `rg -n 'POST /api/teams/:id/members|returns 503|returns 403|returns 404' src/app/api/teams/[id]/members/route.test.ts`
    - `rg -n 'DELETE /api/teams/:id/members/:userId|returns 503|returns 403|returns 400' src/app/api/teams/[id]/members/[userId]/route.test.ts`
    - `rg -n 'displayName|avatarUrl' src/app/api/users/route.test.ts`
    - `npm run test -- src/app/api/teams/[id]/members/route.test.ts src/app/api/teams/[id]/members/[userId]/route.test.ts src/app/api/users/route.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/app/api/teams/[id]/members/route.test.ts src/app/api/teams/[id]/members/[userId]/route.test.ts src/app/api/users/route.test.ts</automated>
  </verify>
  <done>Os contratos das rotas usadas pelo detalhe do time ficaram protegidos por testes focados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar dialog de convite e remoção inline com confirmação</name>
  <read_first>
    - .claude/rules/testing.md
    - .claude/rules/ui-components.md
    - src/components/profile/profile-edit-dialog.tsx
    - src/components/profile/profile-edit-dialog.test.tsx
    - src/components/teams/team-create-form.tsx
    - src/components/teams/team-detail-view.tsx
    - src/components/teams/member-list.tsx
    - src/app/(authenticated)/times/[id]/page.tsx
    - src/lib/team-details.ts
  </read_first>
  <files>src/app/(authenticated)/times/[id]/page.tsx, src/components/teams/team-detail-view.tsx, src/components/teams/invite-member-dialog.tsx, src/components/teams/invite-member-dialog.test.tsx, src/components/teams/member-list.tsx, src/components/teams/member-list.test.tsx</files>
  <behavior>
    - Header `Convidar Membro` abre dialog com input de username.
    - Dialog normaliza `@username` para `username`, faz lookup em `/api/users`, depois POST em `/api/teams/:id/members`.
    - Lista de membros mostra `Remover` apenas para membros não criadores, diferentes do viewer atual, e somente quando `team.type === "solo" ? members.length > 1 : members.length > 2`.
    - Clique em `Remover` troca a linha para estado de confirmação com botões `Confirmar` e `Cancelar` antes do DELETE.
    - Sucesso chama `toast.success("Membro adicionado com sucesso.")` ou `toast.success("Membro removido com sucesso.")` seguido de `router.refresh()`.
    - Falhas mapeiam status 401 -> `Faça login novamente para gerenciar membros.`, 403 -> `Você não pode gerenciar membros deste time.`, 404 do lookup -> `Usuário não encontrado.`, 503 -> `Serviço indisponível. Configure o banco para gerenciar membros.`, fallback -> `data.error ?? "Não foi possível gerenciar o time."`.
  </behavior>
  <action>Editar `src/app/(authenticated)/times/[id]/page.tsx` para passar `viewerId={user.id}` ao detalhe autorizado: `<TeamDetailView viewerId={user.id} {...detail.data} />`. Atualizar `src/components/teams/team-detail-view.tsx` para aceitar props `TeamDetailData & { viewerId: string }` e substituir o botão header stub por `<InviteMemberDialog teamId={data.team.id} />`. Criar `src/components/teams/invite-member-dialog.tsx` como `"use client"` usando os componentes de `src/components/ui/dialog.tsx`, `Input`, `Button`, `useRouter` e `toast`. O submit deve: normalizar `usernameInput.trim().replace(/^@+/, \"\")`, bloquear submit vazio com mensagem inline `Informe um username.`, buscar `/api/users?username=${encodeURIComponent(normalized)}`, usar `user.id` retornado para POST `/api/teams/${teamId}/members`, fechar o dialog em sucesso e chamar `router.refresh()`. Converter `src/components/teams/member-list.tsx` para `"use client"` e ampliar props para `{ members, teamId, teamType, createdBy, viewerId }`. Para cada membro removível, renderizar botão `Remover`; ao clicar, trocar a linha para ações `Confirmar` e `Cancelar`; `Confirmar` chama DELETE `/api/teams/${teamId}/members/${member.userId}` e refresca a página em sucesso. Criar `src/components/teams/invite-member-dialog.test.tsx` e `src/components/teams/member-list.test.tsx` com mocks de `next/navigation`, `sonner` e `global.fetch` cobrindo lookup + add success, status 404/503 do dialog, render condicional do botão `Remover`, confirmação inline, DELETE success e cancelamento sem request.</action>
  <acceptance_criteria>
    - `rg -n 'viewerId=\\{user\\.id\\}|InviteMemberDialog' 'src/app/(authenticated)/times/[id]/page.tsx' src/components/teams/team-detail-view.tsx`
    - `rg -n 'GET /api/users|/api/teams/\\$\\{teamId\\}/members|Membro adicionado com sucesso|Faça login novamente para gerenciar membros' src/components/teams/invite-member-dialog.tsx`
    - `rg -n 'Confirmar|Cancelar|Membro removido com sucesso|viewerId|createdBy|teamType' src/components/teams/member-list.tsx`
    - `rg -n 'router\\.refresh|toast\\.success|toast\\.error' src/components/teams/invite-member-dialog.test.tsx src/components/teams/member-list.test.tsx`
    - `npm run test -- src/components/teams/invite-member-dialog.test.tsx src/components/teams/member-list.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/components/teams/invite-member-dialog.test.tsx src/components/teams/member-list.test.tsx</automated>
  </verify>
  <done>O detalhe do time tem fluxo real de convite e remoção com feedback, confirmação e refresh no local.</done>
</task>

<task type="auto">
  <name>Task 3: Provar runtime com spec Playwright de gerenciamento de membros e acesso negado</name>
  <read_first>
    - e2e/auth-and-scoreboard.spec.ts
    - e2e/helpers/auth.ts
    - src/components/teams/invite-member-dialog.tsx
    - src/components/teams/member-list.tsx
    - src/app/(authenticated)/times/[id]/page.tsx
    - .planning/phases/07-team-details-access-member-actions/07-VALIDATION.md
  </read_first>
  <files>e2e/team-member-actions.spec.ts</files>
  <behavior>
    - Usuário A cria um time, abre `/times/[id]` e adiciona o Usuário B via username.
    - O roster atualizado mostra o novo membro após o refresh automático.
    - Usuário B (ou um terceiro usuário não membro) acessa por URL direta um time do qual não faz parte e vê a tela explícita de acesso negado.
  </behavior>
  <action>Criar `e2e/team-member-actions.spec.ts` com `test.describe(\"team member actions\")` e pelo menos dois cenários: `test(\"manage team members flow\", ...)` e `test(\"team details authorization blocks non-members\", ...)`. Reutilizar `createCredentials`, `signUp`, `login` e `logout` de `e2e/helpers/auth.ts`; não introduzir helpers novos sem necessidade. No cenário de gerenciamento: criar `owner` e `invitee`, fazer signup do owner, criar um time solo pela UI (`/times?tab=create`), abrir o card/nome do time para entrar em `/times/[id]`, acionar `Convidar Membro`, preencher o username do invitee, confirmar e validar a presença do `@username` do invitee na lista após o refresh. No cenário de autorização: usar outro usuário autenticado que não participa do time criado pelo owner, navegar diretamente para a URL salva do time e afirmar a presença dos textos `Voce nao faz parte deste time.` e `Voltar para meus times`. Se o cenário E2E falhar por ausência de `DATABASE_URL` ou `NEXTAUTH_SECRET`, registrar isso no SUMMARY como bloqueio de ambiente sem relaxar o spec.</action>
  <acceptance_criteria>
    - `rg -n 'describe\\(\"team member actions\"\\)|manage team members flow|team details authorization blocks non-members' e2e/team-member-actions.spec.ts`
    - `rg -n 'Convidar Membro|Voltar para meus times|Voce nao faz parte deste time' e2e/team-member-actions.spec.ts`
    - `rg -n 'createCredentials|signUp|login|logout' e2e/team-member-actions.spec.ts`
  </acceptance_criteria>
  <verify>
    <automated>npm run e2e -- --grep "team member actions"</automated>
  </verify>
  <done>Há evidência E2E dedicada para o fluxo de gerenciamento de membros e para o acesso negado por URL direta.</done>
</task>

</tasks>

<verification>
Executar `npm run test -- src/app/api/teams/[id]/members/route.test.ts src/app/api/teams/[id]/members/[userId]/route.test.ts src/app/api/users/route.test.ts src/components/teams/invite-member-dialog.test.tsx src/components/teams/member-list.test.tsx`, `npm run typecheck` e `npm run e2e -- --grep "team member actions"`.
</verification>

<success_criteria>
O detalhe do time oferece ações reais de convite e remoção, com feedback previsível e cobertura automatizada do fluxo autenticado e do acesso negado.
</success_criteria>

<output>
After completion, create `.planning/phases/07-team-details-access-member-actions/07-02-SUMMARY.md`
</output>
