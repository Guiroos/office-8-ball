---
phase: 06-team-creation-flow-wiring
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - e2e/team-create-flow.spec.ts
  - e2e/helpers/auth.ts
autonomous: true
requirements: [TEAM-01]
must_haves:
  truths:
    - "Usuário autenticado consegue criar um time solo pela aba de criação em runtime real."
    - "Após criar, o novo time aparece na lista de times sem recarregar manualmente a página."
    - "Nome criado permanece disponível após reload da página autenticada."
  artifacts:
    - "e2e/team-create-flow.spec.ts valida fluxo completo de criação solo."
    - "e2e/helpers/auth.ts expõe utilitário necessário para login/signup reutilizável no novo spec (sem hardcode de times legados)."
  key_links:
    - "signUp/login helper -> navegação `/times?tab=create` -> submit do formulário."
    - "assert do e2e usa nome dinâmico gerado por timestamp para evitar conflito de unicidade."
    - "sucesso do submit -> presença do time no tab `teams` antes e depois de `page.reload()`."
---

<objective>
Garantir evidência end-to-end do fluxo autenticado de criação solo no runtime.

Purpose: fechar o critério de execução real do TEAM-01 com prova automatizada de integração UI + API.
Output: spec Playwright dedicado para criação solo e gate de verificação de fase.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-team-creation-flow-wiring/06-RESEARCH.md
@.planning/phases/06-team-creation-flow-wiring/06-VALIDATION.md
@.planning/phases/06-team-creation-flow-wiring/06-01-PLAN.md
@e2e/auth-and-scoreboard.spec.ts
@e2e/helpers/auth.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar spec Playwright para fluxo autenticado "create solo team"</name>
  <read_first>
    - e2e/auth-and-scoreboard.spec.ts
    - e2e/helpers/auth.ts
    - src/components/teams/team-create-form.tsx
    - src/app/(authenticated)/times/page.tsx
    - playwright.config.ts
  </read_first>
  <files>e2e/team-create-flow.spec.ts, e2e/helpers/auth.ts</files>
  <behavior>
    - Faz signup/login de usuário dinâmico.
    - Navega para `/times?tab=create`, preenche `team-create-name`, submete `team-create-submit`.
    - Verifica redirecionamento para `/times?tab=teams` e presença do nome criado.
    - Recarrega página e confirma persistência do time criado.
  </behavior>
  <action>Criar `e2e/team-create-flow.spec.ts` com `test.describe(\"team create flow\")` e caso `test(\"create solo team flow\", ...)`. Reutilizar `createCredentials` e `signUp` de `e2e/helpers/auth.ts`. Gerar nome único `const teamName = \`solo-flow-${Date.now()}\`;` e preencher `page.getByTestId(\"team-create-name\")`. Clicar em `page.getByTestId(\"team-create-submit\")`. Aguardar URL `/times\\?tab=teams` e afirmar `page.getByText(teamName.toLowerCase(), { exact: true })` visível. Executar `page.reload()` e repetir a asserção. Se necessário em `e2e/helpers/auth.ts`, adicionar helper reutilizável sem alterar testes existentes (apenas extensão compatível).</action>
  <acceptance_criteria>
    - `rg -n 'describe\\(\"team create flow\"\\)|create solo team flow' e2e/team-create-flow.spec.ts`
    - `rg -n 'solo-flow-\\$\\{Date\\.now\\(\\)\\}' e2e/team-create-flow.spec.ts`
    - `rg -n 'team-create-name|team-create-submit' e2e/team-create-flow.spec.ts`
    - `rg -n '/times\\\\\\?tab=teams|page\\.reload\\(' e2e/team-create-flow.spec.ts`
  </acceptance_criteria>
  <verify>
    <automated>npm run e2e -- --grep "create solo team flow"</automated>
  </verify>
  <done>Fluxo autenticado de criação solo está coberto em E2E sem dependência de IDs fixos de times.</done>
</task>

<task type="auto">
  <name>Task 2: Executar gate de fase para TEAM-01 (unit + typecheck + e2e foco)</name>
  <read_first>
    - .planning/phases/06-team-creation-flow-wiring/06-VALIDATION.md
    - src/components/teams/team-create-form.test.tsx
    - e2e/team-create-flow.spec.ts
  </read_first>
  <files>e2e/team-create-flow.spec.ts</files>
  <action>Executar validação de fase com comandos sequenciais: `npm run test -- src/components/teams/team-create-form.test.tsx src/app/api/teams/route.test.ts`, depois `npm run typecheck`, depois `npm run e2e -- --grep \"create solo team flow\"`. Se o comando E2E falhar por ausência de `DATABASE_URL`/`NEXTAUTH_SECRET`, registrar claramente no SUMMARY como bloqueio de ambiente, sem alterar status-protected da API nem relaxar asserts do spec.</action>
  <acceptance_criteria>
    - Comando unitário retorna exit code 0
    - Comando `npm run typecheck` retorna exit code 0
    - Comando E2E executa o cenário `create solo team flow` (passando ou falhando explicitamente por ambiente ausente)
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/components/teams/team-create-form.test.tsx src/app/api/teams/route.test.ts && npm run typecheck && npm run e2e -- --grep "create solo team flow"</automated>
  </verify>
  <done>Gate automatizado da fase foi executado e resultado de E2E ficou explícito para verificação final do TEAM-01.</done>
</task>

</tasks>

<verification>
Executar `npm run test -- src/components/teams/team-create-form.test.tsx src/app/api/teams/route.test.ts`, `npm run typecheck` e `npm run e2e -- --grep "create solo team flow"`.
</verification>

<success_criteria>
O fluxo "Create solo team" está comprovado por teste E2E autenticado e validado junto aos testes de contrato da UI/API.
</success_criteria>

<output>
After completion, create `.planning/phases/06-team-creation-flow-wiring/06-02-SUMMARY.md`
</output>
