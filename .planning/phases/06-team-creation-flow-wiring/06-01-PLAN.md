---
phase: 06-team-creation-flow-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/teams/team-create-form.tsx
  - src/components/teams/team-create-form.test.tsx
  - src/app/(authenticated)/times/page.tsx
autonomous: true
requirements: [TEAM-01]
must_haves:
  truths:
    - "Usuário autenticado envia `Criar Novo Time` em `/times?tab=create` e o app faz POST real para `/api/teams`."
    - "Fluxo solo envia payload `{ name, type: \"solo\" }` e não envia `secondMemberUserId`."
    - "Erros de validação/autenticação/serviço exibem feedback explícito sem travar a tela."
    - "Sucesso de criação atualiza a listagem de times na própria área `/times`."
  artifacts:
    - "src/components/teams/team-create-form.tsx implementa formulário client-side e chamada POST /api/teams."
    - "src/components/teams/team-create-form.test.tsx cobre payload, sucesso e erros 400/401/503."
    - "src/app/(authenticated)/times/page.tsx substitui o placeholder do tab=create pelo formulário real."
  key_links:
    - "submit handler de team-create-form -> fetch('/api/teams', { method: 'POST' }) -> body `{ name, type: 'solo' }`."
    - "resposta 201 -> toast.success + router.push('/times?tab=teams') + router.refresh()."
    - "resposta não-OK -> parse `{ error?: string }` e mapear 401/503 para mensagens PT-BR específicas."
---

<objective>
Conectar o tab de criação de times ao endpoint real de criação solo com validação e feedback robusto.

Purpose: fechar o gap de runtime do TEAM-01 na UI (`/times?tab=create`) sem alterar contrato de API.
Output: formulário client-side testado e integrado à página `/times`.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-team-creation-flow-wiring/06-RESEARCH.md
@.planning/phases/06-team-creation-flow-wiring/06-VALIDATION.md
@src/app/(authenticated)/times/page.tsx
@src/app/api/teams/route.ts
@src/components/dashboard/use-dashboard-data.ts
@src/components/profile/profile-edit-dialog.tsx
@src/lib/types.ts

<interfaces>
From `src/lib/types.ts`:
```ts
export type TeamResponse = { team: TeamRecord };
export type ApiErrorResponse = { error: string; fieldErrors?: Partial<Record<"username" | "email" | "password", string>>; retryAfterSeconds?: number; };
```

From `src/app/api/teams/route.ts`:
```ts
export async function POST(request: Request)
```
- Success: `201` with `{ team }`
- Validation/auth/service failures used in this phase: `400`, `401`, `503`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar testes de contrato do formulário de criação solo</name>
  <read_first>
    - src/app/api/teams/route.ts
    - src/app/api/teams/route.test.ts
    - src/components/profile/profile-edit-dialog.test.tsx
    - src/components/dashboard.test.tsx
    - .planning/phases/06-team-creation-flow-wiring/06-VALIDATION.md
  </read_first>
  <files>src/components/teams/team-create-form.test.tsx</files>
  <behavior>
    - Teste 1: submit válido chama `fetch("/api/teams")` com method `POST` e body JSON `{ "name": "solo wolves", "type": "solo" }`.
    - Teste 2: resposta `201` dispara feedback de sucesso e chama callbacks de pós-sucesso (refresh/switch tab).
    - Teste 3: resposta `400` mostra mensagem de erro vinda do payload `{ error }`.
    - Teste 4: resposta `401` mostra mensagem fixa `Faça login novamente para criar times.`.
    - Teste 5: resposta `503` mostra mensagem fixa `Serviço indisponível. Configure o banco para criar times.`.
  </behavior>
  <action>Criar `src/components/teams/team-create-form.test.tsx` com Vitest + Testing Library. Mockar `next/navigation` para `useRouter` e expor spies de `push` e `refresh`. Mockar `sonner` para verificar `toast.success` e `toast.error`. Mockar `global.fetch` para cada cenário. Renderizar o componente preenchendo campo `name` com `Solo Wolves`, clicar em botão submit e validar payload serializado em minúsculas/trim (`solo wolves`) com `type: "solo"`. Adicionar asserções explícitas para os branches de status 201, 400, 401 e 503.</action>
  <acceptance_criteria>
    - `rg -n 'describe\\(\"TeamCreateForm\"' src/components/teams/team-create-form.test.tsx`
    - `rg -n '\"/api/teams\"' src/components/teams/team-create-form.test.tsx`
    - `rg -n 'type:\\s*\"solo\"|solo wolves' src/components/teams/team-create-form.test.tsx`
    - `rg -n '401|503|Faça login novamente para criar times|Serviço indisponível\\. Configure o banco para criar times\\.' src/components/teams/team-create-form.test.tsx`
    - `rg -n 'toast\\.success|toast\\.error|router\\.refresh|router\\.push' src/components/teams/team-create-form.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/components/teams/team-create-form.test.tsx</automated>
  </verify>
  <done>Teste do novo formulário existe e cobre payload solo + sucesso + falhas 400/401/503.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar `TeamCreateForm` com validação, submit e status handling</name>
  <read_first>
    - src/components/teams/team-create-form.test.tsx
    - src/components/profile/profile-edit-dialog.tsx
    - src/components/ui/button.tsx
    - src/components/ui/input.tsx
    - src/lib/types.ts
  </read_first>
  <files>src/components/teams/team-create-form.tsx</files>
  <behavior>
    - Valida `name` com zod (`min 1`, `max 50`, trim, lowercase para payload).
    - Envia somente `{ name, type: "solo" }` ao endpoint.
    - Em 201: mostra toast de sucesso, faz `router.push("/times?tab=teams")` e `router.refresh()`.
    - Em 400: mostra erro do payload `{ error }`.
    - Em 401 e 503: usa mensagens fixas definidas no task anterior.
  </behavior>
  <action>Criar `src/components/teams/team-create-form.tsx` como componente `"use client"` com `useState`, `zod`, `toast` (`sonner`) e `useRouter` (`next/navigation`). Declarar schema local `z.object({ name: z.string().min(1, "Nome é obrigatório.").max(50, "Nome pode ter no máximo 50 caracteres.").transform((v) => v.trim().toLowerCase()) })`. Renderizar `Input` com `id=\"team-name\"`, `name=\"name\"`, `data-testid=\"team-create-name\"`; botão submit com `data-testid=\"team-create-submit\"`; estado `loading` refletido no texto `Criando...`/`Criar Time`. No submit, montar body exato `JSON.stringify({ name: parsedName, type: "solo" })`. Para respostas não-OK: mapear `401` e `503` para as mensagens fixas acima; para outros status, ler JSON como `ApiErrorResponse` e usar `error ?? "Não foi possível criar o time."`. Em sucesso (`201`), exibir `toast.success("Time criado com sucesso.")`, limpar campo, chamar `router.push("/times?tab=teams")` e `router.refresh()`.</action>
  <acceptance_criteria>
    - `rg -n '\"use client\"' src/components/teams/team-create-form.tsx`
    - `rg -n 'fetch\\(\"/api/teams\"' src/components/teams/team-create-form.tsx`
    - `rg -n 'type:\\s*\"solo\"' src/components/teams/team-create-form.tsx`
    - `rg -n 'router\\.push\\(\"/times\\?tab=teams\"\\)|router\\.refresh\\(' src/components/teams/team-create-form.tsx`
    - `rg -n 'Faça login novamente para criar times\\.|Serviço indisponível\\. Configure o banco para criar times\\.' src/components/teams/team-create-form.tsx`
    - `rg -n 'data-testid=\"team-create-name\"|data-testid=\"team-create-submit\"' src/components/teams/team-create-form.tsx`
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/components/teams/team-create-form.test.tsx</automated>
  </verify>
  <done>Componente de criação solo funciona contra `/api/teams` e respeita branches de status definidos.</done>
</task>

<task type="auto">
  <name>Task 3: Substituir placeholder de `/times?tab=create` pelo formulário real</name>
  <read_first>
    - src/app/(authenticated)/times/page.tsx
    - src/components/teams/team-create-form.tsx
    - .planning/phases/04-ranking-team-details/04-03-SUMMARY.md
  </read_first>
  <files>src/app/(authenticated)/times/page.tsx</files>
  <action>Editar `src/app/(authenticated)/times/page.tsx` para importar `TeamCreateForm` de `@/components/teams/team-create-form`. No branch `tab === "create"`, remover o parágrafo placeholder (`Fluxo de criação será conectado...`) e renderizar `<TeamCreateForm />`. Manter o restante da estrutura inalterado: tabs por query param, listagem server-side em `tab=teams`, e sidebar CTA existente.</action>
  <acceptance_criteria>
    - `rg -n 'import \\{ TeamCreateForm \\} from \"@/components/teams/team-create-form\"' 'src/app/(authenticated)/times/page.tsx'`
    - `rg -n '<TeamCreateForm\\s*/>' 'src/app/(authenticated)/times/page.tsx'`
    - `rg -n 'Fluxo de criação será conectado' 'src/app/(authenticated)/times/page.tsx'` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/components/teams/team-create-form.test.tsx && npm run typecheck</automated>
  </verify>
  <done>Tab de criação da página `/times` deixou de ser placeholder e usa fluxo real de submit solo.</done>
</task>

</tasks>

<verification>
Executar `npm run test -- src/components/teams/team-create-form.test.tsx src/app/api/teams/route.test.ts` e `npm run typecheck`.
</verification>

<success_criteria>
`/times?tab=create` envia criação solo real via API com feedback de sucesso/erro e atualização de estado da área de times.
</success_criteria>

<output>
After completion, create `.planning/phases/06-team-creation-flow-wiring/06-01-SUMMARY.md`
</output>
