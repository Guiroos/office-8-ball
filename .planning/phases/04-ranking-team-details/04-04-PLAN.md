---
phase: 04-ranking-team-details
plan: "04"
type: execute
wave: 3
depends_on:
  - "04-03"
files_modified:
  - src/components/teams/create-team-form.tsx
  - src/components/teams/create-team-form.test.tsx
  - src/app/(authenticated)/times/page.tsx
  - src/app/api/teams/route.ts
  - src/app/api/teams/route.test.ts
autonomous: true
requirements:
  - TEAM-02
  - RANK-01
gap_closure: true
must_haves:
  truths:
    - "`/times?tab=create` executa criação real de time via POST `/api/teams` (não placeholder)."
    - "Após criar time com sucesso, o usuário retorna para `Meus Times` e visualiza o novo item clicável para `/times/[id]` (per D-05, D-06)."
    - "Fluxo suporta `solo` e `duo` (com `secondMemberUserId` obrigatório para `duo`) em alinhamento com validação da API."
    - "Criação de time dispara revalidação de `/ranking` e `/times` para remover stale state após bootstrap manual."
  artifacts:
    - path: "src/components/teams/create-team-form.tsx"
      provides: "Formulário client-side para criar time e tratar estados de envio/erro/sucesso"
      exports: ["CreateTeamForm"]
    - path: "src/components/teams/create-team-form.test.tsx"
      provides: "Testes de comportamento do formulário (payload, erro, sucesso)"
    - path: "src/app/(authenticated)/times/page.tsx"
      provides: "Aba `Criar Novo Time` conectada ao formulário real"
    - path: "src/app/api/teams/route.ts"
      provides: "POST com revalidatePath para refletir novos times em ranking/times"
    - path: "src/app/api/teams/route.test.ts"
      provides: "Asserções de revalidação sem alterar status protegidos"
  key_links:
    - from: "src/components/teams/create-team-form.tsx"
      to: "/api/teams"
      via: "fetch POST com JSON { name, type, secondMemberUserId? }"
      pattern: "fetch\\(\"/api/teams\""
    - from: "src/app/(authenticated)/times/page.tsx"
      to: "src/components/teams/create-team-form.tsx"
      via: "Render condicional em `tab=create`"
      pattern: "CreateTeamForm"
    - from: "src/app/api/teams/route.ts"
      to: "next/cache"
      via: "revalidatePath('/ranking') e revalidatePath('/times', 'layout') após criação bem-sucedida"
      pattern: "revalidatePath"
---

<objective>
Fechar o gap de criação conectando a aba `/times?tab=create` ao fluxo real de POST `/api/teams`, removendo a dependência de dados externos para iniciar UAT.

Purpose: resolver a causa raiz “missing connected create flow” e permitir que o usuário gere dados válidos diretamente pela UI.
Output: formulário de criação funcional, aba `/times` integrada, e revalidação de cache após criação.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ranking-team-details/04-UAT.md
@.planning/phases/04-ranking-team-details/04-CONTEXT.md
@.planning/phases/04-ranking-team-details/04-03-SUMMARY.md
@.claude/rules/api-patterns.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar formulário CreateTeamForm com contrato explícito da API</name>
  <files>src/components/teams/create-team-form.tsx, src/components/teams/create-team-form.test.tsx</files>
  <behavior>
    - Teste 1: envia `{ name, type: "solo" }` para `/api/teams` e chama callback de sucesso
    - Teste 2: bloqueia submit de `duo` sem `secondMemberUserId`
    - Teste 3: exibe mensagem de erro retornada pela API quando `response.ok === false`
    - Teste 4: estado de submit desabilita botão enquanto request está em andamento
  </behavior>
  <action>
Criar `CreateTeamForm` como client component com os campos:
- `name` (texto)
- `type` (`solo` | `duo`)
- `secondMemberUserId` (obrigatório quando `type === "duo"`)

Regras de implementação:
- usar `fetch("/api/teams", { method: "POST" })` com payload JSON tipado
- manter mensagens de erro em pt-BR
- expor `onCreated?: () => void` para permitir navegação/refresh no caller
- não introduzir dependências novas (usar componentes e utilitários existentes)
- incluir comentário curto `// per D-05/D-06` no bloco do submit para rastreabilidade.
  </action>
  <verify>
    <automated>npm run test -- src/components/teams/create-team-form.test.tsx</automated>
  </verify>
  <done>
    Formulário cria times reais via API e tem cobertura automatizada para sucesso, erro e validação de duo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Conectar aba create de /times ao formulário e retorno para listagem</name>
  <files>src/app/(authenticated)/times/page.tsx</files>
  <action>
Atualizar `/times/page.tsx` para substituir o placeholder de `tab=create` por `CreateTeamForm`.

Comportamento obrigatório:
- ao sucesso de criação, navegar para `/times?tab=teams` e forçar refresh (`router.refresh`) no componente cliente chamado pela página
- preservar comportamento atual de `Meus Times` e CTA lateral (per D-05, D-08)
- manter fallback de indisponibilidade quando `!hasDatabaseUrl()` sem quebrar modo in-memory.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    Aba `Criar Novo Time` deixa de ser placeholder e passa a permitir criação ponta-a-ponta dentro da própria área de times.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Revalidar ranking/times após POST /api/teams sem regressão de status</name>
  <files>src/app/api/teams/route.ts, src/app/api/teams/route.test.ts</files>
  <behavior>
    - Teste 1: criação com sucesso chama `revalidatePath('/ranking')`
    - Teste 2: criação com sucesso chama `revalidatePath('/times', 'layout')`
    - Teste 3: respostas 401/503 e erros de validação existentes permanecem inalterados
  </behavior>
  <action>
No handler `POST` de `src/app/api/teams/route.ts`, adicionar `revalidatePath` imediatamente após criação bem-sucedida.

Atualizar `route.test.ts` para mockar `next/cache` e validar chamadas de revalidação apenas no caminho de sucesso, preservando os contratos de status protegidos exigidos pelo projeto.
  </action>
  <verify>
    <automated>npm run test -- src/app/api/teams/route.test.ts</automated>
  </verify>
  <done>
    Times recém-criados passam a refletir imediatamente em `/times` e `/ranking` sem alterar semântica de erro/autenticação.
  </done>
</task>

</tasks>

<verification>
npm run test -- src/components/teams/create-team-form.test.tsx && npm run test -- src/app/api/teams/route.test.ts && npm run typecheck
</verification>

<success_criteria>
- UAT consegue criar ao menos 2 times ativos apenas pela UI autenticada (`/times?tab=create`).
- O novo time aparece em `Meus Times` e pode abrir `/times/[id]` sem passos manuais fora do app.
- Ranking deixa de depender exclusivamente de seed manual para deixar empty state.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ranking-team-details/04-04-SUMMARY.md`
</output>
