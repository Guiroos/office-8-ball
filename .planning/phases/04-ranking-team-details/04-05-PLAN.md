---
phase: 04-ranking-team-details
plan: "05"
type: execute
wave: 3
depends_on:
  - "04-03"
files_modified:
  - prisma/seed.mjs
  - prisma/seed-uat.mjs
  - prisma/seed-uat.test.ts
  - .planning/phases/04-ranking-team-details/04-UAT.md
autonomous: true
requirements:
  - RANK-01
  - RANK-02
  - RANK-03
  - RANK-04
  - TEAM-02
gap_closure: true
must_haves:
  truths:
    - "Existe bootstrap reproduzível para UAT que cria dados mínimos (times ativos + confrontos) para validar ranking e H2H."
    - "Seed padrão sem perfil explícito continua sem criar times (preserva decisão histórica de runtime-created teams)."
    - "Com perfil UAT ativo, o ambiente passa a ter dados suficientes para clicar no ranking, abrir `/times/[id]` e validar H2H."
    - "Pré-condições de UAT ficam documentadas e executáveis por comando único."
  artifacts:
    - path: "prisma/seed-uat.mjs"
      provides: "Fixture de UAT com usuários, times ativos e partidas mínimas para ranking/H2H"
      exports: ["UAT_SEED_PROFILE", "buildUatSeedData"]
    - path: "prisma/seed.mjs"
      provides: "Seleção de perfil de seed com modo padrão no-op e perfil `uat` opt-in"
      contains: "SEED_PROFILE"
    - path: "prisma/seed-uat.test.ts"
      provides: "Teste automatizado da composição mínima de fixtures UAT"
    - path: ".planning/phases/04-ranking-team-details/04-UAT.md"
      provides: "Preflight de setup para execução de UAT com dados"
  key_links:
    - from: "prisma/seed.mjs"
      to: "prisma/seed-uat.mjs"
      via: "seleção de perfil (`SEED_PROFILE=uat` ou flag equivalente)"
      pattern: "seed-uat"
    - from: ".planning/phases/04-ranking-team-details/04-UAT.md"
      to: "prisma/seed.mjs"
      via: "passo de precondição antes dos testes funcionais"
      pattern: "SEED_PROFILE=uat"
---

<objective>
Fechar o gap de setup de dados criando um bootstrap de UAT reproduzível para ranking/team-details sem depender de inserções manuais ad-hoc.

Purpose: resolver a causa raiz “lack of test data/setup” de forma repetível e auditável.
Output: perfil de seed UAT opt-in, teste da fixture e instruções de preflight no 04-UAT.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ranking-team-details/04-UAT.md
@.planning/phases/04-ranking-team-details/04-CONTEXT.md
@.planning/phases/04-ranking-team-details/04-03-SUMMARY.md
@prisma/schema.prisma
@prisma/seed.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir fixture UAT mínima para ranking e H2H</name>
  <files>prisma/seed-uat.mjs, prisma/seed-uat.test.ts</files>
  <behavior>
    - Teste 1: fixture inclui ao menos 3 times ativos (solo/duo mistos) ligados ao usuário testador
    - Teste 2: fixture inclui ao menos 1 confronto direto entre dois times para validar seção H2H
    - Teste 3: fixture garante que ranking consiga exibir W/L, winRate, streak e totalMatches sem empty-state global
  </behavior>
  <action>
Criar `prisma/seed-uat.mjs` com um builder puro (`buildUatSeedData`) que retorne os lotes mínimos necessários:
- usuários (incluindo testador)
- times ativos
- membros de time
- partidas suficientes para ranking e H2H

Criar `prisma/seed-uat.test.ts` validando apenas estrutura e cardinalidade da fixture (sem acesso real ao banco), para manter feedback rápido e estável.

Observação obrigatória na action: manter alinhamento com a decisão prévia de “times criados em runtime”, tornando este bootstrap estritamente opt-in para UAT.
  </action>
  <verify>
    <automated>npm run test -- prisma/seed-uat.test.ts</automated>
  </verify>
  <done>
    Fixture UAT existe, é testada e prova cobertura mínima dos cenários bloqueados no UAT.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrar seed profile `uat` no seed principal sem alterar default no-op</name>
  <files>prisma/seed.mjs</files>
  <action>
Atualizar `prisma/seed.mjs` para suportar perfil selecionável (`SEED_PROFILE=uat` ou flag `--profile uat`) com estas regras:
- comportamento padrão continua no-op (mensagem atual), preservando a decisão histórica do projeto
- perfil `uat` executa upserts/inserts usando os dados de `buildUatSeedData`
- incluir modo `--dry-run` que imprime resumo do que seria criado sem gravar no banco (para verificação em CI/local)
- não alterar schema nem adicionar dependências.
  </action>
  <verify>
    <automated>node prisma/seed.mjs --profile uat --dry-run</automated>
  </verify>
  <done>
    Seed principal continua seguro por padrão e passa a ter caminho explícito de bootstrap para UAT.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atualizar 04-UAT.md com preflight obrigatório de dados</name>
  <files>.planning/phases/04-ranking-team-details/04-UAT.md</files>
  <action>
Adicionar seção de preflight no `04-UAT.md` antes dos testes:
1) comando para bootstrap de dados UAT
2) confirmação esperada (times visíveis no ranking e em `/times`)
3) observação de cleanup/reset quando necessário

Registrar explicitamente que os testes 1/3/5/6 dependem dessa preparação para evitar novo falso-negativo “sem times”.
  </action>
  <verify>
    <automated>rg -n \"SEED_PROFILE=uat|preflight|pré-condição|sem times\" .planning/phases/04-ranking-team-details/04-UAT.md</automated>
  </verify>
  <done>
    UAT da fase 04 passa a ter setup reproduzível e rastreável, reduzindo reincidência dos mesmos gaps.
  </done>
</task>

</tasks>

<verification>
npm run test -- prisma/seed-uat.test.ts && node prisma/seed.mjs --profile uat --dry-run
</verification>

<success_criteria>
- Um comando de bootstrap prepara dados suficientes para executar todos os cenários principais de ranking/team-details.
- A ausência de times deixa de bloquear UAT funcional.
- Seed padrão continua no-op quando nenhum perfil de UAT é solicitado.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ranking-team-details/04-05-SUMMARY.md`
</output>
