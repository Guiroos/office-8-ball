---
phase: 04-ranking-team-details
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/ranking/podium-card.tsx
  - src/components/ranking/standings-row.tsx
  - src/components/ranking/type-tabs.tsx
  - src/components/ranking/ranking-view.tsx
  - src/components/ranking/ranking-view.test.tsx
  - src/app/(authenticated)/ranking/page.tsx
autonomous: true
requirements:
  - RANK-01
  - RANK-02
  - RANK-03
  - RANK-04
must_haves:
  truths:
    - "`/ranking` shows active teams ordered by the ranking contract from plan 04-01."
    - "Top 3 render in podium order 2 | 1 | 3 with center emphasis for rank 1 (per D-01)."
    - "Rows below podium show W/L, winRate, current streak, total matches (per D-04 and RANK-02..04)."
    - "Tabs `Solo` and `Duplas` filter by URL query `?type=solo|duo` (per D-02)."
    - "Ranking page is RSC and fetches data server-side (per D-11)."
    - "In-memory unavailable state copy is distinct from empty category state."
  artifacts:
    - path: "src/components/ranking/podium-card.tsx"
      provides: "Top-3 card visual with medal labels and stats"
      exports: ["PodiumCard"]
    - path: "src/components/ranking/standings-row.tsx"
      provides: "Compact row for rank 4+ linking to team detail"
      exports: ["StandingsRow"]
    - path: "src/components/ranking/type-tabs.tsx"
      provides: "Accessible tab navigation backed by query string"
      exports: ["TypeTabs"]
    - path: "src/components/ranking/ranking-view.tsx"
      provides: "View orchestration for podium/list/empty states"
      exports: ["RankingView"]
    - path: "src/components/ranking/ranking-view.test.tsx"
      provides: "UI tests for podium order, states, and links"
    - path: "src/app/(authenticated)/ranking/page.tsx"
      provides: "RSC page using listAllTeamsWithStats and searchParams type filter"
      contains: "listAllTeamsWithStats"
  key_links:
    - from: "src/app/(authenticated)/ranking/page.tsx"
      to: "src/lib/ranking.ts"
      via: "listAllTeamsWithStats(parsedType)"
      pattern: "listAllTeamsWithStats"
    - from: "src/components/ranking/standings-row.tsx"
      to: "src/app/(authenticated)/times/[id]/page.tsx"
      via: "Link href={`/times/${team.id}`}`"
      pattern: "href=.*times.*team\.id"
---

<objective>
Implement the ranking UI and route wiring with deterministic behavior, URL-driven filtering, and explicit test coverage for the review feedback.

Purpose: deliver RANK-01..RANK-04 with the visual contract from context decisions and prevent regressions in podium order/filter behavior.
Output: ranking components, ranking page RSC, and ranking view tests.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ranking-team-details/04-CONTEXT.md
@.planning/phases/04-ranking-team-details/04-REVIEWS.md
@.planning/phases/04-ranking-team-details/04-UI-SPEC.md
@.planning/phases/04-ranking-team-details/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ranking components with decision-locked layout and accessible tabs</name>
  <files>src/components/ranking/podium-card.tsx, src/components/ranking/standings-row.tsx, src/components/ranking/type-tabs.tsx, src/components/ranking/ranking-view.tsx</files>
  <read_first>
    - src/components/primitives/stat-tile.tsx
    - src/components/primitives/section-header.tsx
    - src/components/dashboard/dashboard-hero.tsx
    - src/app/globals.css
    - src/components/ranking/podium-card.tsx (if already present)
    - src/components/ranking/standings-row.tsx (if already present)
    - src/components/ranking/type-tabs.tsx (if already present)
    - src/components/ranking/ranking-view.tsx (if already present)
    - .planning/phases/04-ranking-team-details/04-UI-SPEC.md
    - .planning/phases/04-ranking-team-details/04-CONTEXT.md
  </read_first>
  <action>
Implement the four components with these concrete rules:

`podium-card.tsx` (per D-01, D-04):
- Props: `{ team: RankedTeam }`
- Rank badges exact text: `🏆 1º Lugar`, `🥈 2º Lugar`, `🥉 3º Lugar`
- Show 4 metrics: `Vitórias`, `Derrotas`, `Taxa de Vitória`, `Sequência Atual`
- Link wrapper: `href={`/times/${team.id}`}`

`standings-row.tsx` (per D-01, D-04):
- Props: `{ team: RankedTeam }`
- Render rank `#${team.rank}`, team name, type badge (`Solo` or `Duplas`), `wins`, `losses`, `winRate`, `currentStreak`, and `totalMatches`
- Link wrapper: `href={`/times/${team.id}`}`

`type-tabs.tsx` (per D-02, addresses review accessibility concern):
- Keep as client component
- Use semantic links instead of plain buttons:
  - `Todos` -> `/ranking`
  - `Solo` -> `/ranking?type=solo`
  - `Duplas` -> `/ranking?type=duo`
- Add `aria-current="page"` to the active tab link

`ranking-view.tsx`:
- Props:
  - `teams: RankedTeam[]`
  - `activeType: "all" | "solo" | "duo"`
  - `mode: "available" | "unavailable"`
- Podium ordering in UI must be exact `2, 1, 3` when at least 3 teams exist
- Rows below podium render teams with rank >= 4
- Empty state copy (available/no data): `Nenhum time encontrado nesta categoria`
- Unavailable copy (in-memory/no DB): `Ranking indisponível em modo de desenvolvimento`
- Include comments in code marking `per D-01`, `per D-02`, `per D-04` for traceability.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "🏆 1º Lugar" src/components/ranking/podium-card.tsx` returns one match
    - `grep -n "Nenhum time encontrado nesta categoria" src/components/ranking/ranking-view.tsx` returns one match
    - `grep -n "Ranking indisponível em modo de desenvolvimento" src/components/ranking/ranking-view.tsx` returns one match
    - `grep -n "aria-current" src/components/ranking/type-tabs.tsx` returns at least one match
    - `grep -n "href=\"/ranking?type=solo\"\|type=solo" src/components/ranking/type-tabs.tsx` returns one match
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    Ranking components are implemented with locked layout/filters and accessibility semantics.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire /ranking RSC and add behavioral tests for podium/filter/states</name>
  <files>src/app/(authenticated)/ranking/page.tsx, src/components/ranking/ranking-view.test.tsx</files>
  <read_first>
    - src/app/(authenticated)/ranking/page.tsx
    - src/lib/ranking.ts
    - src/components/ranking/ranking-view.tsx
    - src/components/ranking/ranking-view.test.tsx (if already present)
    - src/lib/auth.ts
    - .planning/phases/04-ranking-team-details/04-REVIEWS.md
  </read_first>
  <behavior>
    - `/ranking` page is an async server component (per D-11)
    - `searchParams.type` supports only `solo` and `duo`; all other values map to `all`
    - Page passes `mode="unavailable"` when `!hasDatabaseUrl()` and `mode="available"` otherwise
    - Tests assert podium render order `2 | 1 | 3`, tab filtering contract, and both empty-state copies
  </behavior>
  <action>
Update `src/app/(authenticated)/ranking/page.tsx`:
- Signature: `export default async function RankingPage({ searchParams }: { searchParams?: Promise<{ type?: string }> })`
- Resolve params with `const resolved = (await searchParams) ?? {}`
- Parse `type` with exact whitelist: `solo`, `duo`; otherwise `undefined`
- Derive `activeType` as `"solo" | "duo" | "all"`
- Call `listAllTeamsWithStats(parsedType)`
- Use `hasDatabaseUrl()` from `@/lib/auth` to set `mode`
- Render `<RankingView teams={teams} activeType={activeType} mode={mode} />`

Create/update `src/components/ranking/ranking-view.test.tsx` with tests for:
- podium order: render 3+ teams and assert visual order is rank 2 first, rank 1 second, rank 3 third
- list rows render from rank 4 onward
- empty state copy for available mode
- unavailable state copy for unavailable mode
- each team row/card links to `/times/{id}`

Use Testing Library and Vitest patterns already used in project test files.
  </action>
  <verify>
    <automated>npm run test -- src/components/ranking/ranking-view.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "listAllTeamsWithStats" src/app/(authenticated)/ranking/page.tsx` returns one match
    - `grep -n "searchParams" src/app/(authenticated)/ranking/page.tsx` returns at least one match
    - `grep -n "2 \| 1 \| 3\|rank 2\|rank 1\|rank 3" src/components/ranking/ranking-view.test.tsx` returns at least one match
    - `grep -n "Nenhum time encontrado nesta categoria" src/components/ranking/ranking-view.test.tsx` returns one match
    - `grep -n "Ranking indisponível em modo de desenvolvimento" src/components/ranking/ranking-view.test.tsx` returns one match
    - `npm run test -- src/components/ranking/ranking-view.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    Ranking route is wired as RSC and ranking behavior tests cover review-critical interactions.
  </done>
</task>

</tasks>

<verification>
npm run test -- src/components/ranking/ranking-view.test.tsx && npm run typecheck
</verification>

<success_criteria>
- Ranking page fulfills D-01, D-02, D-04, D-11 and requirements RANK-01..RANK-04.
- Query param filtering and podium order are deterministic and tested.
- Unavailable and empty states are distinct and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ranking-team-details/04-02-SUMMARY.md`
</output>
