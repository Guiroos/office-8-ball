---
phase: 04-ranking-team-details
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ranking.ts
  - src/lib/ranking.test.ts
  - src/app/api/matches/route.ts
  - src/app/api/matches/route.test.ts
autonomous: true
requirements:
  - RANK-01
  - RANK-02
  - RANK-03
  - RANK-04
must_haves:
  truths:
    - "listAllTeamsWithStats() returns all active teams ordered by wins desc, winRate desc, name asc (stable tie-break)."
    - "Ranking type filter uses team.type (solo|duo) while stats still use full match history for each returned team."
    - "Archived teams are excluded from ranking results."
    - "In-memory mode (without DATABASE_URL) returns [] without throwing."
    - "POST /api/matches revalidates /ranking and /times subtree after successful match creation."
  artifacts:
    - path: "src/lib/ranking.ts"
      provides: "Ranking domain function and RankedTeam type"
      exports: ["listAllTeamsWithStats", "RankedTeam"]
    - path: "src/lib/ranking.test.ts"
      provides: "Unit tests for filtering, ordering, rank assignment, archived exclusion, in-memory fallback"
    - path: "src/app/api/matches/route.ts"
      provides: "POST handler cache invalidation with revalidatePath"
      contains: "revalidatePath('/ranking')"
    - path: "src/app/api/matches/route.test.ts"
      provides: "Route-level assertion that revalidation executes on successful POST"
  key_links:
    - from: "src/lib/ranking.ts"
      to: "src/lib/stats.ts"
      via: "computeTeamStats(team.id, allMatches)"
      pattern: "computeTeamStats"
    - from: "src/app/api/matches/route.ts"
      to: "next/cache"
      via: "revalidatePath('/ranking') and revalidatePath('/times', 'layout')"
      pattern: "revalidatePath"
---

<objective>
Create the ranking data foundation and cache invalidation behavior required by Phase 4 UI plans.

Purpose: unblock ranking/team pages with a single reusable data contract and remove stale-cache risk after match creation.
Output: ranking domain module + tests, and revalidation in matches POST route + tests.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ranking-team-details/04-CONTEXT.md
@.planning/phases/04-ranking-team-details/04-REVIEWS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ranking domain contract and stable ordering</name>
  <files>src/lib/ranking.ts, src/lib/ranking.test.ts</files>
  <read_first>
    - src/lib/stats.ts
    - src/lib/types.ts
    - src/lib/data.ts
    - src/lib/data.test.ts
    - src/lib/teams.ts
    - src/lib/ranking.ts (if already present)
    - src/lib/ranking.test.ts (if already present)
    - .planning/phases/04-ranking-team-details/04-CONTEXT.md
    - .planning/phases/04-ranking-team-details/04-REVIEWS.md
  </read_first>
  <behavior>
    - Returns RankedTeam[] for active teams only (per D-03, D-11)
    - Optional filter accepts only "solo" or "duo" (per D-02)
    - Sort order is wins DESC, winRate DESC, name ASC (addresses review concern: missing stable tie-break)
    - Rank field is 1-indexed after sorting
    - Returns [] when DATABASE_URL is absent (in-memory mode)
  </behavior>
  <action>
Create or update `src/lib/ranking.ts` with:
- `export type RankedTeam = TeamRecord & TeamStats & { rank: number }`
- `export async function listAllTeamsWithStats(type?: "solo" | "duo"): Promise<RankedTeam[]>`
- Query Prisma teams with `where: { status: "active", ...(type ? { type } : {}) }` and `include: { members: true }`
- Query ALL matches for returned team IDs using OR on `teamAId`/`teamBId` with no `.take()`
- Normalize matches to `MatchRecord` and compute stats with `computeTeamStats`
- Sort with exact comparator order:
  1) `b.wins - a.wins`
  2) `b.winRate - a.winRate`
  3) `a.name.localeCompare(b.name, "pt-BR")`
- Assign `rank = index + 1`
- Return `[]` immediately when `!process.env.DATABASE_URL`

Create/update `src/lib/ranking.test.ts` with automated cases for:
- in-memory fallback (`DATABASE_URL` removed => `[]`)
- archived team exclusion
- type filter for both `solo` and `duo`
- ordering by wins, then winRate, then name
- zero-match team inclusion with zeroed stats
- rank assignment starting at 1

In the test descriptions include the text `per D-02` and `per D-03` to keep traceability to locked decisions.
  </action>
  <verify>
    <automated>npm run test -- src/lib/ranking.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/ranking.ts` exports `listAllTeamsWithStats` and `RankedTeam`
    - `grep -n "localeCompare" src/lib/ranking.ts` returns one match
    - `grep -n "status: \"active\"" src/lib/ranking.ts` returns one match
    - `grep -n "type\?: \"solo\" \| \"duo\"" src/lib/ranking.ts` returns one match
    - `npm run test -- src/lib/ranking.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Ranking data contract exists, stable sorting is explicit, and ranking tests are green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add route cache revalidation for ranking and team subtree</name>
  <files>src/app/api/matches/route.ts, src/app/api/matches/route.test.ts</files>
  <read_first>
    - src/app/api/matches/route.ts
    - src/app/api/matches/route.test.ts
    - src/lib/auth.ts
    - .planning/phases/04-ranking-team-details/04-CONTEXT.md
    - .planning/phases/04-ranking-team-details/04-REVIEWS.md
  </read_first>
  <behavior>
    - POST /api/matches calls `revalidatePath('/ranking')` (per D-12)
    - POST /api/matches calls `revalidatePath('/times', 'layout')` to cover `/times/[id]` freshness (addresses review concern: stale dynamic detail route)
    - Existing status code behavior remains unchanged (401/403/404/422/503)
  </behavior>
  <action>
Update `src/app/api/matches/route.ts`:
- Add `import { revalidatePath } from "next/cache"`
- In POST handler, after `const response = await createMatch(...)` and before the `201` return, add:
  - `revalidatePath("/ranking");`
  - `revalidatePath("/times", "layout");`

Update `src/app/api/matches/route.test.ts`:
- Mock `next/cache` and assert both calls happen exactly once for successful POST.
- Keep all existing guard tests and protected status semantics unchanged.
  </action>
  <verify>
    <automated>npm run test -- src/app/api/matches/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "revalidatePath\(\"/ranking\"\)" src/app/api/matches/route.ts` returns one match
    - `grep -n "revalidatePath\(\"/times\", \"layout\"\)" src/app/api/matches/route.ts` returns one match
    - `grep -n "next/cache" src/app/api/matches/route.test.ts` returns one match
    - `npm run test -- src/app/api/matches/route.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Match POST revalidation is implemented and tested for both ranking and times subtree freshness.
  </done>
</task>

</tasks>

<verification>
npm run test -- src/lib/ranking.test.ts && npm run test -- src/app/api/matches/route.test.ts && npm run typecheck
</verification>

<success_criteria>
- Ranking data source is stable and deterministic for equal-score teams.
- Ranking filtering and archived exclusion follow locked decisions (D-02, D-03).
- Cache invalidation after match creation updates both `/ranking` and `/times/[id]` paths.
- No protected API status regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ranking-team-details/04-01-SUMMARY.md`
</output>
