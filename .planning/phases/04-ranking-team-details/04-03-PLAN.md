---
phase: 04-ranking-team-details
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/lib/team-details.ts
  - src/lib/team-details.test.ts
  - src/components/teams/team-card.tsx
  - src/components/teams/member-list.tsx
  - src/components/teams/recent-matches-list.tsx
  - src/components/teams/h2h-section.tsx
  - src/components/teams/team-detail-view.tsx
  - src/app/(authenticated)/times/page.tsx
  - src/app/(authenticated)/times/[id]/page.tsx
autonomous: true
requirements:
  - TEAM-02
must_haves:
  truths:
    - "`/times` renders two tabs (`Meus Times`, `Criar Novo Time`) and shows team cards in the first tab (per D-05)."
    - "`/times` renders right-column CTA `Criar Novo Time` when no team is selected from the list context (per D-08)."
    - "`/times/[id]` is accessible to any authenticated user for active teams (explicit policy to resolve review auth gap)."
    - "`/times/[id]` shows team header, roster with member names + role, and stats with explicit `wins`, `losses`, `totalMatches`, ranking position, and last 3 matches (per D-07 and Phase 4 SC-3)."
    - "H2H dropdown rivals are sourced only from viewer membership teams (excluding current team), preloaded with primary rival inside this constrained set, and show W/L, win rate, and lastMatchDate (per D-09, D-10)."
    - "H2H computations run server-side; client receives only precomputed rival summaries (addresses review payload concern)."
  artifacts:
    - path: "src/lib/team-details.ts"
      provides: "Server-side assembler for team detail data, including rivals and H2H summaries"
      exports: ["getTeamDetailData", "TeamDetailData", "TeamH2HSummary"]
    - path: "src/lib/team-details.test.ts"
      provides: "Tests for access policy, archived handling, primary rival selection, and lastMatchDate derivation"
    - path: "src/components/teams/member-list.tsx"
      provides: "Roster rendering with member display name/username and role labels"
      exports: ["MemberList"]
    - path: "src/components/teams/h2h-section.tsx"
      provides: "Client selector that renders precomputed server summaries only"
      exports: ["H2HSection"]
    - path: "src/app/(authenticated)/times/[id]/page.tsx"
      provides: "RSC route using getTeamDetailData and 404 for missing/archived teams"
      contains: "getTeamDetailData"
  key_links:
    - from: "src/app/(authenticated)/times/[id]/page.tsx"
      to: "src/lib/team-details.ts"
      via: "getTeamDetailData(teamId, viewerId)"
      pattern: "getTeamDetailData"
    - from: "src/lib/team-details.ts"
      to: "src/lib/stats.ts"
      via: "computeTeamStats and computeHeadToHead"
      pattern: "computeHeadToHead"
    - from: "src/components/teams/h2h-section.tsx"
      to: "src/lib/team-details.ts"
      via: "TeamH2HSummary payload (no allMatches prop)"
      pattern: "lastMatchDate"
---

<objective>
Implement team list/detail pages with explicit access policy, server-side detail assembly, and H2H behavior aligned with locked decisions and review feedback.

Purpose: satisfy TEAM-02 without stale/ambiguous contracts and avoid client-side recomputation payload bloat.
Output: team detail data helper + tests, teams components, and `/times` + `/times/[id]` routes.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ranking-team-details/04-CONTEXT.md
@.planning/phases/04-ranking-team-details/04-REVIEWS.md
@.planning/phases/04-ranking-team-details/04-UI-SPEC.md
@.planning/phases/04-ranking-team-details/04-01-SUMMARY.md

<interfaces>
From src/lib/teams.ts:
```ts
export async function listUserTeams(userId: string, includeArchived?: boolean): Promise<TeamRecord[]>;
export async function getTeamById(teamId: string): Promise<TeamRecord | null>;
```

From src/lib/stats.ts:
```ts
export function computeTeamStats(teamId: string, allMatches: MatchRecord[]): TeamStats;
export function computeHeadToHead(teamAId: string, teamBId: string, allMatches: MatchRecord[]): HeadToHeadStats;
```

From src/lib/ranking.ts (plan 04-01):
```ts
export async function listAllTeamsWithStats(type?: "solo" | "duo"): Promise<RankedTeam[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build server-side team detail assembler with explicit access policy</name>
  <files>src/lib/team-details.ts, src/lib/team-details.test.ts</files>
  <read_first>
    - src/lib/team-details.ts (if already present)
    - src/lib/team-details.test.ts (if already present)
    - src/lib/teams.ts
    - src/lib/stats.ts
    - src/lib/ranking.ts
    - src/lib/types.ts
    - src/lib/prisma.ts
    - src/app/api/teams/[id]/route.ts
    - .planning/phases/04-ranking-team-details/04-CONTEXT.md
    - .planning/phases/04-ranking-team-details/04-REVIEWS.md
  </read_first>
  <behavior>
    - Access policy: any authenticated user can view detail of active team in `/times/[id]` (addresses review concern #2)
    - Returns null for non-existent or archived team
    - Includes roster display data with `displayName ?? username` and role (`Criador` or `Membro`) (per D-07)
    - Includes ranking position from `listAllTeamsWithStats()`
    - Precomputes rivals H2H on server, including `lastMatchDate` derived from `recentMatchIds` (per D-09, D-10)
    - Rivals source for H2H is viewer membership teams only (`listUserTeams(viewerId)`), excluding `teamId` (per D-09)
    - Computes `primaryRivalId` as rival with max `totalMatches` inside the viewer-membership rival set; fallback first rival in that same set when all zero
  </behavior>
  <action>
Create `src/lib/team-details.ts` with these exact exports:

```ts
export type TeamH2HSummary = {
  rivalTeamId: string;
  rivalTeamName: string;
  teamAWins: number;
  teamBWins: number;
  totalMatches: number;
  winRate: number;
  lastMatchDate: string | null;
};

export type TeamMemberView = {
  userId: string;
  displayName: string;
  username: string;
  role: "Criador" | "Membro";
};

export type TeamDetailData = {
  team: TeamRecord;
  rankingPosition: number | null;
  stats: TeamStats;
  recentMatches: MatchRecord[];
  members: TeamMemberView[];
  rivals: { id: string; name: string; type: "solo" | "duo" }[];
  h2hByRival: Record<string, TeamH2HSummary>;
  primaryRivalId: string | null;
};

export async function getTeamDetailData(teamId: string, viewerId: string): Promise<TeamDetailData | null>
```

Implementation details:
- Do not call `listMatches(viewerId)` here because that is membership-scoped.
- Query active team by `teamId` directly via Prisma with `include: { members: true }`; return `null` if missing or `status !== "active"`.
- Query data needed for ranking/detail with separated sources:
  - ranking position from global active ranking list (`listAllTeamsWithStats()`)
  - rivals from viewer membership teams (`listUserTeams(viewerId)`), filtered to exclude current `teamId`, and excluding archived teams
  - matches where `teamAId = teamId OR teamBId = teamId` for recent list
  - matches where either side is `teamId` and other side in viewer-membership rival IDs for H2H summaries
- Build member profile map from `prisma.user.findMany({ where: { id: { in: memberIds } }, select: { id: true, username: true, displayName: true } })`.
- Convert `playedAt` to ISO strings.
- For `lastMatchDate`, derive by taking `h2h.recentMatchIds[0]` and mapping to the corresponding match ISO date.
- Add inline comment `// per D-09/D-10` above H2H summary assembly.

Create `src/lib/team-details.test.ts` covering:
- returns null for archived team
- returns null for missing team
- returns members with display names and creator/member roles
- includes ranking position when team exists in ranking list
- limits rivals to viewer membership teams only (excluding current team)
- selects primary rival by max `totalMatches` within viewer-membership rivals
- includes `lastMatchDate` in ISO string when H2H exists

Use `vi.mock("@/lib/prisma")` and existing in-memory-safe patterns from repository tests.
  </action>
  <verify>
    <automated>npm run test -- src/lib/team-details.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function getTeamDetailData" src/lib/team-details.ts` returns one match
    - `grep -n "lastMatchDate" src/lib/team-details.ts` returns at least one match
    - `grep -n "displayName" src/lib/team-details.ts` returns at least one match
    - `grep -n "primaryRivalId" src/lib/team-details.ts` returns at least one match
    - `npm run test -- src/lib/team-details.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Team detail data contract is server-first, explicit, and fully tested for the review's high-risk gaps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement teams UI components against server precomputed contracts</name>
  <files>src/components/teams/team-card.tsx, src/components/teams/member-list.tsx, src/components/teams/recent-matches-list.tsx, src/components/teams/h2h-section.tsx, src/components/teams/team-detail-view.tsx</files>
  <read_first>
    - src/components/teams/team-card.tsx (if already present)
    - src/components/teams/member-list.tsx (if already present)
    - src/components/teams/recent-matches-list.tsx (if already present)
    - src/components/teams/h2h-section.tsx (if already present)
    - src/components/teams/team-detail-view.tsx (if already present)
    - src/components/primitives/stat-tile.tsx
    - src/components/primitives/section-header.tsx
    - src/lib/team-details.ts
    - .planning/phases/04-ranking-team-details/04-UI-SPEC.md
    - .planning/phases/04-ranking-team-details/04-CONTEXT.md
  </read_first>
  <action>
Build component contract with no client-side H2H recomputation:

`team-card.tsx`:
- clickable card linking to `/times/${team.id}`
- show initials avatar, team name, type badge (`Solo`/`Duplas`)

`member-list.tsx`:
- props: `members: TeamMemberView[]`
- render member display name and `@username`
- render role badge exactly `Criador` or `Membro`

`recent-matches-list.tsx`:
- props: `matches: MatchRecord[]`, `teamId: string`, `teamNameById: Record<string, string>`
- render only first 3 matches, pt-BR date formatting
- show `Ver histórico completo` as non-navigating secondary text (deferred to Phase 5)

`h2h-section.tsx` (client component):
- props:
  - `rivals: { id: string; name: string; type: "solo" | "duo" }[]`
  - `h2hByRival: Record<string, TeamH2HSummary>`
  - `defaultRivalId: string | null`
- DO NOT import `computeHeadToHead`
- DO NOT receive `allMatches`
- render selector + stats from `h2hByRival[selectedId]`
- show empty copy `Nenhuma partida entre estes times` when selected rival has `totalMatches = 0`

`team-detail-view.tsx`:
- props include `rankingPosition: number | null`
- stats tiles include: `Total Wins`, `Total Losses`, `Total de Partidas`, `Win Rate %`, `Posição no ranking`, `Sequência Atual` (per D-07 and SC-3)
- include `Convidar Membro` button in header

Add comments in this task implementation noting:
- `per D-07` for roster/stats block
- `per D-09/D-10` for H2H block
- `addresses review concern: server-side H2H summary` above `H2HSection` integration
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "computeHeadToHead" src/components/teams/h2h-section.tsx` returns no match
    - `grep -n "allMatches" src/components/teams/h2h-section.tsx` returns no match
    - `grep -n "Convidar Membro" src/components/teams/team-detail-view.tsx` returns one match
    - `grep -n "Posição no ranking" src/components/teams/team-detail-view.tsx` returns one match
    - `grep -n "Total Losses" src/components/teams/team-detail-view.tsx` returns one match
    - `grep -n "Total de Partidas" src/components/teams/team-detail-view.tsx` returns one match
    - `grep -n "Criador\|Membro" src/components/teams/member-list.tsx` returns at least two matches
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    Teams UI uses the server detail contract and no longer depends on client-side full-match payloads.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire /times and /times/[id] pages with tabs, CTA sidebar, and detail access policy</name>
  <files>src/app/(authenticated)/times/page.tsx, src/app/(authenticated)/times/[id]/page.tsx</files>
  <read_first>
    - src/app/(authenticated)/times/page.tsx
    - src/app/(authenticated)/times/[id]/page.tsx (if already present)
    - src/lib/auth.ts
    - src/lib/teams.ts
    - src/lib/team-details.ts
    - src/components/teams/team-card.tsx
    - src/components/teams/team-detail-view.tsx
    - .planning/phases/04-ranking-team-details/04-CONTEXT.md
    - .planning/phases/04-ranking-team-details/04-REVIEWS.md
  </read_first>
  <action>
Update `src/app/(authenticated)/times/page.tsx` (per D-05, D-08, D-11):
- Convert from placeholder to async RSC
- Read `searchParams.tab` with whitelist `teams|create`, default `teams`
- Render two tab links:
  - `/times?tab=teams` label `Meus Times`
  - `/times?tab=create` label `Criar Novo Time`
- `tab=teams` view:
  - fetch current user teams with `listUserTeams(user.id)` when `hasDatabaseUrl()`
  - render list using `TeamCard`
- Right sidebar block (desktop) with CTA heading `Criar Novo Time` and button linking `/times?tab=create`
- `tab=create` view: render explicit placeholder copy `Fluxo de criação será conectado ao formulário existente de times nesta fase.`

Create/update `src/app/(authenticated)/times/[id]/page.tsx`:
- async RSC with `params: Promise<{ id: string }>` and `await params`
- if `!hasDatabaseUrl()` render unavailable state copy (not 404): `Detalhes do time indisponíveis sem banco configurado.`
- load authenticated user and call `getTeamDetailData(id, user.id)`
- if helper returns null -> `notFound()`
- render `TeamDetailView` with helper payload
- `/times/[id]` view must surface `losses` and `totalMatches` from the helper payload in rendered stats (SC-3 alignment)
- This route must not enforce team membership; it follows explicit authenticated-public detail policy for active teams (addresses review concern #2).
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Meus Times" src/app/(authenticated)/times/page.tsx` returns one match
    - `grep -n "Criar Novo Time" src/app/(authenticated)/times/page.tsx` returns at least two matches (tab + sidebar CTA)
    - `grep -n "searchParams" src/app/(authenticated)/times/page.tsx` returns at least one match
    - `grep -n "getTeamDetailData" src/app/(authenticated)/times/[id]/page.tsx` returns one match
    - `grep -n "await params" src/app/(authenticated)/times/[id]/page.tsx` returns one match
    - `grep -n "notFound()" src/app/(authenticated)/times/[id]/page.tsx` returns one match
    - `grep -n "losses\\|totalMatches" src/app/(authenticated)/times/[id]/page.tsx src/components/teams/team-detail-view.tsx` returns matches proving both fields are wired and rendered
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    `/times` and `/times/[id]` routes are wired to the new data contract and satisfy locked UX decisions.
  </done>
</task>

</tasks>

<verification>
npm run test -- src/lib/team-details.test.ts && npm run typecheck
</verification>

<success_criteria>
- TEAM-02 is complete with detail page data assembled server-side and rendered in the required sections.
- Access policy for `/times/[id]` is explicit and implemented.
- H2H includes `lastMatchDate` and primary rival preload constrained to viewer membership rivals (excluding current team), without shipping full match history to the client.
- `/times/[id]` explicitly renders `losses` and `totalMatches` as part of the required stats block (SC-3).
</success_criteria>

<output>
After completion, create `.planning/phases/04-ranking-team-details/04-03-SUMMARY.md`
</output>
