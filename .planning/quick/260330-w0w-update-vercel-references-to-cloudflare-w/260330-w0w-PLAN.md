---
phase: quick
plan: 260330-w0w
type: execute
wave: 1
depends_on: []
files_modified:
  - techspec/git-conventions.md
  - .claude/skills/release-workflow/references/deploy-workflow.md
  - .claude/rules/architecture.md
  - CLAUDE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "No doc file references Vercel as the deployment platform"
    - "Required GitHub secrets list matches deploy-production-tag.yml exactly"
    - "deploy-workflow.md step sequence matches the actual workflow steps"
    - "@vercel/speed-insights is not referenced anywhere in docs"
  artifacts:
    - path: "techspec/git-conventions.md"
      provides: "Updated release flow and secrets list (Cloudflare Workers)"
    - path: ".claude/skills/release-workflow/references/deploy-workflow.md"
      provides: "Rewritten workflow reference matching deploy-production-tag.yml"
    - path: ".claude/rules/architecture.md"
      provides: "Updated CI/Deploy constraint (Cloudflare Workers, not Vercel)"
    - path: "CLAUDE.md"
      provides: "Updated deploy platform references"
  key_links:
    - from: "techspec/git-conventions.md"
      to: ".github/workflows/deploy-production-tag.yml"
      via: "secrets list and step 6 description"
      pattern: "CLOUDFLARE_API_TOKEN|CLOUDFLARE_ACCOUNT_ID|BETTER_AUTH_SECRET|BETTER_AUTH_URL"
    - from: ".claude/skills/release-workflow/references/deploy-workflow.md"
      to: ".github/workflows/deploy-production-tag.yml"
      via: "step sequence"
      pattern: "wrangler deploy|prisma:deploy|Sync Cloudflare"
---

<objective>
Update all stale documentation that still references Vercel as the deployment platform. The actual deploy workflow already correctly deploys to Cloudflare Workers via wrangler — only the docs are out of date. Also remove the stale @vercel/speed-insights reference from CLAUDE.md (the package was already removed from package.json and has no src/ imports).

Purpose: Keep docs trustworthy. Stale Vercel references cause confusion when setting up secrets, diagnosing failures, or onboarding.
Output: Four updated doc files with Cloudflare Workers as the canonical platform.
</objective>

<execution_context>
@/home/guiroos/Documentos/Projects/office-8-ball/.claude/get-shit-done/workflows/execute-plan.md
@/home/guiroos/Documentos/Projects/office-8-ball/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.github/workflows/deploy-production-tag.yml
@wrangler.jsonc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite deploy-workflow.md and update architecture.md</name>
  <files>.claude/skills/release-workflow/references/deploy-workflow.md, .claude/rules/architecture.md</files>
  <action>
Rewrite `.claude/skills/release-workflow/references/deploy-workflow.md` to match the actual workflow in `.github/workflows/deploy-production-tag.yml`. The new content must reflect:

**Trigger:** push of tag matching `v*` OR `workflow_dispatch`. Pushing to master does NOT trigger a deploy.

**Exact step sequence:**
1. Checkout repository
2. Setup Node.js 20 with npm cache
3. `npm ci` — install dependencies
4. `npm run prisma:generate` — regenerate Prisma client
5. `npm run prisma:deploy` — apply pending migrations to production DB
6. `npm run build` — build production bundle (via vinext)
7. Sync Cloudflare runtime secrets via `npx wrangler secret put` for each of: DATABASE_URL, BETTER_AUTH_SECRET, BETTER_AUTH_URL, NEXT_PUBLIC_APP_ENV
8. `npm run deploy:cloudflare` (which runs `wrangler deploy`) — deploy to Cloudflare Workers

**Critical note:** migrations run at step 5, before the build. If the migration fails, build and deploy do not run.

**Required GitHub secrets** (production environment):

| Secret | Purpose |
|--------|---------|
| `DATABASE_URL` | Production Postgres connection string |
| `BETTER_AUTH_SECRET` | better-auth session secret |
| `BETTER_AUTH_URL` | better-auth base URL |
| `CLOUDFLARE_API_TOKEN` | Cloudflare CLI authentication |
| `CLOUDFLARE_ACCOUNT_ID` | Cloudflare account identifier |

**Failure recovery sections:**
- Migration failed: inspect via `gh run view <run-id> --log-failed`, fix in new branch, re-tag with new version. Never re-run with same tag after destructive schema change.
- Cloudflare auth failed: confirm `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` are set in the `production` environment. Regenerate token at dash.cloudflare.com if expired.
- Tag not triggering workflow: verify tag starts with `v`. Check `on.push.tags` pattern in deploy-production-tag.yml.

In `.claude/rules/architecture.md`, update the "CI / Deploy Changes" section. Change:
  "Constraint: keep Vercel as deployment platform; GitHub is for validation only."
to:
  "Constraint: keep Cloudflare Workers as deployment platform; GitHub is for validation only."
  </action>
  <verify>
    <automated>grep -r "Vercel" .claude/skills/release-workflow/references/deploy-workflow.md .claude/rules/architecture.md && echo "FAIL: stale Vercel refs" || echo "PASS: no stale Vercel refs"</automated>
  </verify>
  <done>deploy-workflow.md reflects the actual wrangler-based workflow. architecture.md constraint references Cloudflare Workers. No Vercel references remain in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Update techspec/git-conventions.md and CLAUDE.md</name>
  <files>techspec/git-conventions.md, CLAUDE.md</files>
  <action>
In `techspec/git-conventions.md`:

1. In the "Fluxo oficial de release" section, update step 6:
   Change: "Aguardar o workflow `Deploy Production Tag` publicar a producao na Vercel"
   To: "Aguardar o workflow `Deploy Production Tag` publicar a producao no Cloudflare Workers"

2. In the paragraph after the commands block, change:
   "o GitHub Actions executa o deploy de producao na Vercel"
   To: "o GitHub Actions executa o deploy de producao no Cloudflare Workers"

3. In "Checklist de release v1.0.0", update step 7:
   Change: "Confirmar a execucao do workflow `Deploy Production Tag`" — no change needed here (platform-neutral), but verify it does not say "Vercel".

4. In "Secrets e pre-requisitos de deploy", replace the entire secrets list:
   Remove: NEXTAUTH_SECRET, VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID
   Keep: DATABASE_URL
   Add: BETTER_AUTH_SECRET, BETTER_AUTH_URL, CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID

5. Update the explanatory paragraph after the secrets list to remove references to Vercel CLI and Vercel publishing, replacing with Cloudflare Workers equivalents. New text should convey:
   - o workflow de producao aplica `prisma migrate deploy` antes do build
   - o Cloudflare Workers nao publica automaticamente a cada commit
   - a publicacao em producao fica amarrada a tags `v*`

In `CLAUDE.md`:

1. Line ~160: Change "**Deploy:** Vercel + GitHub Actions" to "**Deploy:** Cloudflare Workers + GitHub Actions"
2. Line ~173: The "Node.js (via Next.js/Vercel)" entry under Runtime — change to "Node.js (via Next.js/Cloudflare Workers)"
3. Line ~215: Remove the entire line "- @vercel/speed-insights 2.0.0 - Core Web Vitals monitoring" (package was already removed from package.json and has no src/ imports)
4. Line ~237: Change "- Vercel (primary deployment platform)" to "- Cloudflare Workers (primary deployment platform)"
  </action>
  <verify>
    <automated>grep -n -i "vercel" techspec/git-conventions.md CLAUDE.md</automated>
  </verify>
  <done>techspec/git-conventions.md secrets list matches deploy-production-tag.yml. CLAUDE.md has no Vercel platform references and no @vercel/speed-insights entry. Any remaining "Vercel" occurrences are only in historical/context-neutral passages (e.g. the workflow name "Deploy Production Tag" itself, or vercel.json references if applicable).</done>
</task>

</tasks>

<verification>
Run after both tasks:

```bash
grep -rn "VERCEL_TOKEN\|VERCEL_ORG_ID\|VERCEL_PROJECT_ID\|NEXTAUTH_SECRET\|speed-insights" \
  techspec/git-conventions.md \
  .claude/skills/release-workflow/references/deploy-workflow.md \
  .claude/rules/architecture.md \
  CLAUDE.md
```

Expected: no output (all stale references removed).

```bash
grep -n "CLOUDFLARE_API_TOKEN\|CLOUDFLARE_ACCOUNT_ID\|BETTER_AUTH_SECRET\|BETTER_AUTH_URL" \
  techspec/git-conventions.md \
  .claude/skills/release-workflow/references/deploy-workflow.md
```

Expected: at least one match per file confirming new secrets are documented.
</verification>

<success_criteria>
- No file in scope references Vercel as deployment platform
- Required secrets documented in git-conventions.md and deploy-workflow.md match those in deploy-production-tag.yml exactly (DATABASE_URL, BETTER_AUTH_SECRET, BETTER_AUTH_URL, CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID)
- deploy-workflow.md step sequence matches the actual workflow steps (prisma:generate → prisma:deploy → build → wrangler secret put × 4 → wrangler deploy)
- @vercel/speed-insights not mentioned in any updated file
- No workflow YAML files were touched
</success_criteria>

<output>
After completion, create `.planning/quick/260330-w0w-update-vercel-references-to-cloudflare-w/260330-w0w-SUMMARY.md`
</output>
```
