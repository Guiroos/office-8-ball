interface:
  display_name: "Git Worktree Flow"
  short_description: "Create repo-aligned branches and commits"
  default_prompt: "Use $git-worktree-flow to read the repo git conventions and prepare a branch and relevant commits from the current worktree."
policy:
  allow_implicit_invocation: false
